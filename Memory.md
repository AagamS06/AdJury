# AdJury — Project Memory (Onboarding & Status)

> **Purpose:** The single catch-up doc for anyone (human or AI) joining AdJury. Read **this first** — it tells you what AdJury is, what's done, what's in progress, and what's next, so you don't have to read the whole codebase to get oriented.
>
> **Keep this file current.** Update the status tables at the end of every work session (per [Rules.md](Rules.md) §9).
>
> **Last updated:** 2026-09-23 (Day 27 — Brand voice in reviews)

---

## 1. What is AdJury? (30-second version)

An **AI content-critique SaaS**. Businesses paste marketing content (ad copy, social posts, emails, landing pages); **five AI "juror" personas** each score and critique it from a different angle, returning per-juror scores, an aggregate score + verdict, flagged issues, and **suggested rewrites**. It's a **review/QA layer**, not a content generator — humans stay in control.

**Core cost mechanic:** ONE cost-efficient model (default **Claude Haiku 4.5**) called with **five different system prompts**, instead of five paid providers.

**The 5 jurors:** Brand Voice Guardian · Compliance & Legal Flagger · Target Audience Fit · SEO / Discoverability · "Would I Stop Scrolling".

**Read next:** [PRD.md](PRD.md) (what & why) → [Architecture.md](Architecture.md) (how it's built) → [Rules.md](Rules.md) (conventions) → [Phases.md](Phases.md) (plan) → [DailyPlan.md](DailyPlan.md) (per-day tasks) → [Design.md](Design.md) (look & feel).

> **🤖 Automated daily build:** a cloud routine runs **every day at 4:30am (Australia/Sydney)** on **Opus 4.8**. Each run executes the **next unchecked day** in [DailyPlan.md](DailyPlan.md) and **opens a PR** (never merges, never touches secrets). Review and merge PRs to advance. Manage/disable at https://claude.ai/code/routines.

---

## 2. Key Facts (don't re-derive these)

- **Owner / dev:** Aagam Shah (aagamshah250506@gmail.com). Solo, ~10 hrs/week, first-year CS (AI) student. Strong Python/C++/JS; building full-stack depth.
- **Timeline:** 12 weeks, 5 phases. Started Week 1 (~late Aug 2026).
- **Recommended stack:** Next.js 15 (App Router) + TypeScript + Tailwind + shadcn/ui + Supabase (Postgres/Auth/RLS) + one AI provider behind `lib/ai/client.ts`. See [Architecture.md](Architecture.md) §1. *(This is a recommendation the owner can override — confirm before deep work.)*
- **Output contract:** the juror JSON schema in [PRD.md](PRD.md) §7 is the thing everything depends on. Don't break it silently.
- **Constraints that shape decisions:** tight AI budget (one model, caching, rate limits); multi-tenant safety is non-negotiable; enterprise-credible design, not "cutesy."
- **Palette:** Navy `#0A1F44`, Royal Blue `#1E40AF`, Burgundy `#6B1F2A`, Black `#0B0B0F`, White. See [Design.md](Design.md).

---

## 3. Current Status

**Phase:** 3 (multi-tenant & dashboard) — **Weeks 1–3 complete (Days 1–21); Week 4 started (Day 22)**. Week 2 delivered the full submit → scorecard → history → detail web flow with loading/error/empty states, closed out by a Day 14 hardening pass (UI-logic + accessibility-invariant tests, a scorecard/history consistency fix). Week 3 hardened the core engine: Day 15 (suggested-rewrite UX: copy-to-clipboard + a legible before/after rewrite diff), Day 16 (configurable per-company juror weights: scoring reads a resolved weight config; the aggregate honors custom weights, default equal unchanged), Day 17 (per-plan review rate limiting: `/api/reviews` returns 429 with reset info once a company exceeds its plan's rolling-window limit, enforced before the model fan-out), Day 18 (cost guardrails: a shared `src/lib/ai/cost.ts` owns the content size cap, a per-review token budget, and a redacted usage logger; `createReview` rejects oversized/over-budget content before the fan-out and logs content-free usage on success), Day 19 (provider resilience: a pure `src/lib/ai/resilience.ts` adds a per-call timeout + exponential backoff that retries only transient provider failures, threaded into each juror's model call via an `AbortSignal`; a stuck/failing juror degrades to a clean `error` slot while the rest still return), Day 20 (platform-aware prompts: the SEO and "stop scrolling" jurors now adapt expectations to the selected platform via a pure `src/lib/ai/personas/platform-guidance.ts`, injected into those two jurors' user prompts at review time), and Day 21 (Week 3 hardening: a shared `REWRITE_GUIDANCE` calibration fragment so no juror's rewrite fabricates claims/disclaimers, a new orchestrator integration suite pinning the Day 19/20 invariants end-to-end, and expanded compliance-veto verdict coverage). **Week 4 (multi-tenant foundations) is underway:** Day 22 (company onboarding — a first-admin flow that turns the name-only auto-provisioned company into a described, usable company context: name/industry/plan-tier form, gated so an admin whose company hasn't been set up is guided into `/onboarding`) and Day 23 (team management UI — an admin-only `/team` page listing the company's members and roles, tenant-safe via the server-resolved session + RLS, with the roster read core enforcing the admin role as defense-in-depth), and Day 24 (invitations — an admin invites a teammate by email and the invitee follows a tokenised `/invite/<token>` link to join the *existing* company; only a SHA-256 hash of the bearer token is stored, email delivery is stubbed/logged with the link surfaced in-app, and the accept core adds the member to the invite's company with the invite's role after enforcing email-match/expiry/single-company on the server), and Day 25 (role management — an admin promotes/demotes teammates from the `/team` table, authorized server-side: a pure `wouldRemoveLastAdmin` guard and injectable `changeMemberRole` core enforce admin-only + target-in-company + a no-lockout invariant, the write is company-scoped, and the per-row toggle is a small client island), and Day 26 (brand profile CRUD — an admin-only `/brand` editor to create/edit the company's tone/style guide, saved to `brand_profiles`; a pure input contract + injectable `saveBrandProfile` core resolve create-vs-edit and enforce admin-only on the server, and the write goes through the request-scoped client so the existing admin-write RLS policy is the DB-level guard — "admin-only via RLS"), and Day 27 (brand voice in reviews — the company's stored tone/style guide is loaded server-side in `createReview` (scoped to the session company via `getBrandProfileByCompany` through the request-scoped anon client, never the client — Rules.md §5) and threaded into the review as `brand_context`, which the orchestrator now injects into the **Brand Voice Guardian's prompt only** (a new `resolveBrandContext` helper resolves the guide to trimmed text or null; a whitespace-only/absent guide falls back to the juror inferring a professional baseline); the cost model was corrected so brand context is counted **once** (it reaches a single juror) rather than multiplied across the fan-out, keeping the token budget from contradicting the content limit; the brand read **fails open** — a read blip degrades to a brand-less review rather than failing the request). Next is **Day 28 — Week 4 hardening**.

### ✅ Done
- Project documentation set authored: PRD, Architecture, Rules, Phases, Design, this Memory doc, and README.
- Core concept, 5-juror definitions, scoring rubric anchors, and JSON output contract specified in [PRD.md](PRD.md).
- Tech stack, data model, folder structure, and AI orchestration flow defined in [Architecture.md](Architecture.md).
- **Project scaffolded** (Next.js 15 + TS + Tailwind): `package.json`, `tsconfig`, `next/tailwind/postcss` configs, `.env.example`, `.gitignore`.
- **Zod juror schema** implemented — `src/lib/schema/juror.ts` (the contract from PRD §7).
- **5 persona system prompts + rubrics** — `src/lib/ai/personas/*` (base + one file per juror).
- **AI orchestrator** — `src/lib/ai/orchestrator.ts`: fan-out to 5 jurors, parse + Zod-validate (retry once), single-juror failures degrade gracefully.
- **Provider-abstracted model client** — `src/lib/ai/client.ts`: Anthropic when `AI_API_KEY` set, deterministic **offline mock** otherwise (so tests/demo run with no key).
- **Scoring & verdict** — `src/lib/scoring.ts`: weighted aggregate + compliance-veto verdict logic.
- **DB migration** — `supabase/migrations/0001_init.sql`: full schema + RLS policies; `seed.sql` dev data.
- **End-to-end proof** — `npm run demo` (sample ad copy → 5 jurors → JSON in console) and `npm test` both green; `npm run typecheck` clean.
- **DB data-access layer (Day 2)** — `src/types/db.ts` (hand-written row types mirroring the migration) and `src/lib/db/queries.ts`: typed helpers for companies, users, brand_profiles, and reviews/persona_scores (`insertReviewWithScores` with rollback, `getReviewById` with tenancy scoping, `listReviewsByCompany`). `scripts/db-smoke.ts` (+ `npm run db:smoke`) inserts and reads back a review against a dev Supabase, or prints manual steps + exits 0 when no keys are present. Unit test for the pure juror→row mapper.
- **Auth foundation (Day 3)** — Supabase email/password auth wired with `@supabase/ssr` (cookie sessions). `src/lib/auth/`: SSR browser + server client factories, `session.ts` `getSessionContext()` (resolves auth user → company + role, server-side, never from the client), `provisioning.ts` `provisionCompanyForNewUser()` (signup creates a `companies` row + first **admin** `users` row with rollback, run via the service-role client to bootstrap past RLS), Zod `schema.ts`, and `actions.ts` server actions (`signInAction`/`signUpAction`/`signOutAction`). `src/middleware.ts` refreshes the session cookie so logins persist (route protection deferred to Day 4). On-brand `/login` + `/signup` pages (`src/app/(auth)/`) using Design.md tokens with labels/focus rings; the landing page shows signed-in state + sign-out. `npm test` now **22 tests** green; `npm run build` compiles.
- **Role-based access (Day 4)** — Route protection in two layers that never trust the client (Rules.md §5). `src/lib/auth/access.ts` is a pure, fully-tested policy module: protected/admin/auth path matchers and `evaluateAccess(principal, {requireAdmin})` returning an allow/deny decision (unauth → `/login`, member on an admin route → `/dashboard`). `src/lib/auth/guard.ts` wraps it for Server Components — `requireSession()` and `requireAdmin()` resolve the real role via `getSessionContext()` and `redirect()` on denial (fails closed if Supabase env is absent). `middleware.ts` now does a coarse gate too (anon → login off protected URLs; signed-in → dashboard off auth pages) while preserving refreshed cookies on redirects; role checks stay server-side since the role lives in the DB, not the token. New `src/app/(dashboard)/` route group: `layout.tsx` (`requireSession`, nav that hides Brand/Team from members), `dashboard/` home (any user), and admin-only `brand/` + `team/` placeholders (each calls `requireAdmin`; full CRUD is Days 23/26). Landing page gained a Dashboard link when signed in. New `tests/auth-access.test.ts` (10 tests) proves the DoD: unauth redirected, member cannot reach admin routes. `npm test` now **32 tests** green; typecheck clean; `npm run build` compiles.
- **Review endpoint (Day 5)** — First Phase 2 API slice: `POST /api/reviews` (`src/app/api/reviews/route.ts`) Zod-validates the body, runs the 5-juror orchestrator, persists `reviews` + `persona_scores`, and returns the composed `ReviewResult`. Identity is tenant-safe (Rules.md §5): `company_id`/`submitted_by` come from `getSessionContext()`, never the body — the client can only send content fields via the new `ReviewRequestSchema` (a `pick` of `ReviewInputSchema`; `brand_context` stays server-side and null until Day 27). The orchestration + error mapping live in a testable core `src/lib/reviews/create-review.ts` (`createReview(body, {session, persist, client?})`) so the route is a thin wrapper that only wires real deps; the write goes through the **service-role client** with an explicit `companyId` because `persona_scores` has no client INSERT policy in the RLS migration. Error handling per Rules.md §6: 401 unauth, 400 bad/empty/oversized input, **502 when every juror fails (nothing persisted)**, 500 on write failure (no false success, logged with redacted context). New `tests/reviews-endpoint.test.ts` (8 tests, offline mock client + injected persist) proves the DoD incl. tenancy-spoof rejection. `npm test` now **40 tests** green; typecheck clean; `npm run build` compiles (`/api/reviews` is a dynamic route).
- **Fetch & list endpoints (Day 6)** — Read side of the reviews API, both company-scoped. `GET /api/reviews/[id]` (`src/app/api/reviews/[id]/route.ts`) returns one review reconstructed from `reviews` + `persona_scores`; `GET /api/reviews` (added to the existing route file) lists the company's reviews newest-first for the history view. Logic lives in a testable core `src/lib/reviews/read-reviews.ts` (`getReviewForCompany`, `listReviewsForCompany`) with the DB reads injected, plus pure row→DTO mappers: `scoreRowToJurorSlot` (the inverse of Day 5's `jurorSlotToScoreRow` — a malformed `ok` row downgrades to an `error` slot so a persisted review never claims a result it can't back up), `reviewWithScoresToPersisted` (composes a `PersistedReview` mirroring `ReviewResult`, minus `model` which the table doesn't store, jurors emitted in canonical persona order), and `reviewRowToSummary` (a thin `ReviewSummary` with no `content_text` — the list doesn't leak content). Tenancy (Rules.md §5): the company is taken from the session and passed explicitly to every query; reads go through the **request-scoped anon client** so RLS is the primary guard (both `reviews` and `persona_scores` have SELECT policies) with the session `companyId` as a second layer. A row that is absent OR another company's reads as **404** (never reveal cross-tenant existence — Rules.md §6); 401 unauth, 500 on query failure (redacted log, generic message). List `limit` is clamped to [1,100]. New `tests/reviews-read.test.ts` (14 tests) proves the DoD incl. the 404-vs-other-company case and session-derived scoping. `npm test` now **54 tests** green; typecheck clean; `npm run build` compiles (`/api/reviews` and `/api/reviews/[id]` are dynamic routes).
- **Week 1 hardening (Day 7)** — Test + prompt hardening pass that closes out Week 1; no new product surface. **Tenancy tests deepened** with `tests/reviews-tenancy.test.ts` (8 tests): a faithful, chainable fake Supabase query builder records every `.eq()`/op so the *data layer itself* is proven to apply the `company_id` filter — `getReviewById` filters by both `id` and `company_id` (and omits `company_id` only when a service-role caller passes none), `listReviewsByCompany` filters + orders newest-first — plus the previously-untested **write rollback** (a failed `persona_scores` insert deletes the orphaned `reviews` row, Rules.md §6) and an end-to-end **cross-tenant isolation** check through the read core (company B gets **404, not 403**, for company A's review). **Endpoint coverage expanded** in `tests/reviews-endpoint.test.ts` (now 10): unknown `content_type` enum value → 400; submitted `content_type`/`platform` flow through to persistence untampered. **Light persona calibration:** added a shared `CONFIDENCE_GUIDANCE` block, composed into all five persona system prompts via `composeSystemPrompt` (`src/lib/ai/personas/base.ts`) — the `confidence` field was in the contract but had no prompt guidance, so it was reported inconsistently; the rubric ties `high/medium/low` to certainty of the *score* (a low score can be high-confidence). New `tests/personas.test.ts` (9 tests) pins persona wiring: the five schema personas in canonical order, no duplicate names, and every prompt embeds its machine-name, the shared anchors, the confidence rubric, and the JSON output contract (Rules.md §3 — prompt edits are reviewed code changes). `npm test` now **73 tests** green (10 files); typecheck clean; `npm run build` compiles.

#### Week 1 summary (Days 1–7) — Foundation & first review slice
Week 1 delivered the full Phase-1 foundation and the first end-to-end review slice. **Engine:** Zod juror contract (PRD §7), five persona system prompts + shared anchors/confidence rubric, a fan-out orchestrator (parallel, validate-and-retry-once, single-juror failures degrade to `error`), weighted scoring with compliance veto, and a provider-abstracted model client with a deterministic **offline mock** so everything builds/tests with no key. **Data:** Supabase migration + RLS, hand-written row types, and a typed data-access layer (`insertReviewWithScores` with rollback, tenant-scoped `getReviewById`, `listReviewsByCompany`). **Auth & tenancy:** `@supabase/ssr` cookie sessions, `getSessionContext()` (resolves company + role server-side), signup provisioning (company + admin), two-layer route protection (coarse middleware gate + server-component `requireSession`/`requireAdmin`), on-brand login/signup and dashboard route group. **API:** `POST /api/reviews` (run + persist, tenant-safe identity from the session) and the read side `GET /api/reviews` + `/api/reviews/[id]` (company-scoped, 404-not-403 cross-tenant). Ends Week 1 at **73 tests green**, typecheck clean, build compiling. **Still credential-blocked (owner action):** no live model or Supabase in the cloud build, so auth/DB paths are code-complete but unverified against a real project, and persona wording is uncalibrated against real model output. **Next:** Week 2 — submission UI, scorecard + JurorCard, verdict/score visual system, history and detail pages.

- **Submission UI (Day 8)** — First Week 2 slice: the `/review` page where a logged-in user submits content and receives a review payload. `src/app/(dashboard)/review/page.tsx` (Server Component, `requireSession()` gate) renders a client form `src/components/review/review-form.tsx` (content textarea with a live character counter, content-type + platform `<select>`s, client-side validation, `POST /api/reviews`). Form logic is factored into a pure, testable module `src/lib/reviews/review-form.ts` — the option lists, `validateReviewForm` (mirrors the shared `ReviewRequestSchema`; trims content, maps an empty platform to `null`, returns plain-language per-field errors), and `submitReview(data, fetchImpl)` (posts, then **re-validates the success payload against `ReviewResultSchema`** so a malformed response is surfaced as an error, not rendered). Client validation is a convenience only — the API remains the trust boundary and re-derives tenancy from the session (Rules.md §5/§6). On success a **deliberately minimal** result panel shows the aggregate score, verdict, per-juror score/`Needs retry`, and a raw-JSON `<details>` — the full Scorecard/JurorCard visual system stays for Days 9–10 (not pulled forward). Accessibility per Design.md §6: real `<label>`s, `aria-invalid` + `aria-describedby` on errored fields, `role="alert"`/`role="status"`, Design.md tokens throughout. Nav gained a **New review** link (all users) and the dashboard a quick-submit card. New `tests/review-form.test.ts` (12 tests: validation happy/edge paths + injected-`fetch` success/error/malformed/network mapping). `npm test` now **85 tests** green (11 files); typecheck clean; `npm run build` compiles (`/review` dynamic).

- **Scorecard + JurorCard (Day 9)** — Turned the Day 8 minimal result panel into the full Design.md §5 scorecard, rendered from the `ReviewResult` the form already receives. Pure presentation logic lives in `src/lib/reviews/scorecard-view.ts` (testable in the node env, mirroring the `review-form.ts` split): `formatAggregate` (null → em dash for a never-scored review), `verdictLabel`/`verdictTone`, `scoreTone` (score band → semantic tone), `severityLabel`, `confidenceLabel`. Colour is mapped to Tailwind classes at the component layer (`src/components/review/tone-classes.ts`) so the lib stays framework-agnostic. Two new **pure, hook-free** presentational components: `src/components/review/scorecard.tsx` (Navy header bar, aggregate score large + left-aligned, verdict pill top-right, then a JurorCard grid) and `src/components/review/juror-card.tsx` (persona name H3, colour-mapped score chip, confidence line, one-line summary, an expandable `<details>` issues list with severity labels + excerpt/explanation, and the suggested rewrite in a tinted panel). Both take the loose shape shared by `ReviewResult` and the Day 6 `PersistedReview` (nullable `aggregate_score`/`verdict`), so the Day 12 detail page can reuse them without changes. A juror slot with `status: "error"` renders its own "No result" state, never a fake score (Rules.md §6). Accessibility (Design.md §6): meaning is never colour-only — the score chip carries its number, the verdict pill carries its word (with an `sr-only` "Verdict:" prefix), and severities carry a text badge. `ReviewForm` now stacks vertically and renders `<Scorecard>` full-width on success (raw-JSON `<details>` kept as a dev affordance). New `tests/scorecard-view.test.ts` (8 tests) pins the mappings. `npm test` now **93 tests** green (12 files); typecheck clean; `npm run build` compiles (`/review` dynamic, 17.1 kB). **Deferred to Day 10 (not pulled forward):** the full 5-tier score→colour system (the distinct 3–4 "warning-deep" tier + the compliance-flag burgundy override), issue-severity *colour* styling, and the a11y AA verification pass — Day 9's chip/pill mapping is intentionally coarse.

- **Review history (Day 11)** — The `/history` page: a company-scoped list of past reviews (content type/platform, date, aggregate score, verdict), newest first, each row linking to its Day 12 detail page (`/review/[id]`). `src/app/(dashboard)/history/page.tsx` is a Server Component gated by `requireSession()`; it reuses the **Day 6 read core** `listReviewsForCompany` — the company comes from the server-resolved session (never the client — Rules.md §5) and is passed explicitly to `listReviewsByCompany` through the **request-scoped anon client**, so RLS is the primary tenancy guard exactly as in `GET /api/reviews`. A load failure renders a plain-language `role="alert"` message instead of crashing (Rules.md §6). Presentation logic is a pure module `src/lib/reviews/history-view.ts` (mirroring the `review-form.ts`/`scorecard-view.ts` split): `formatHistoryDate` (deterministic UTC-parts format, so it's locale/timezone-independent for tests; unparseable → "Unknown date"), `contentTypeLabel`/`platformLabel` (reuse the submission-form option lists; unknown values fall back to themselves), `reviewDetailHref`, and `toHistoryRow` (projects a thin `ReviewSummary` into a row view-model, **reusing** `verdictLabel`/`verdictTone` and `scoreTone`/`scoreTierLabel`/`formatAggregate` so a history row's pill and chip read identically to the scorecard; a never-scored review reads neutral with no tier word rather than being forced into a band — Rules.md §6). The hook-free `src/components/review/history-list.tsx` renders a quiet bordered/zebra table (Design.md §5 "Tables/history") with tabular numerals, an `overflow-x-auto` wrapper, and an **empty state** ("No reviews yet" → CTA to `/review`). A11y (Design.md §6): real `<table>` semantics (`<caption>` sr-only, `<th scope="col">`), the row link carries an `sr-only` "view review" suffix, and colour never travels alone (score chip carries its number + `sr-only` tier word, verdict pill carries its word with an `sr-only` "Verdict:" prefix). Nav gained a **History** link (all users) and the dashboard a "View history" card. New `tests/history-view.test.ts` (11 tests: date formatting incl. the invalid case, the label maps, and `toHistoryRow` scored/never-scored/extreme-band projections). `npm test` now **115 tests** green (13 files); typecheck clean; `npm run build` compiles (`/history` dynamic, 161 B). **Deferred (not pulled forward):** the detail page the rows link to is Day 12; the full dashboard "recent reviews" widget is Day 52.

- **Review detail page (Day 12)** — The `/review/[id]` page that lights up the Day 11 history-row links: it reconstructs a stored review's full scorecard from `reviews` + `persona_scores`. `src/app/(dashboard)/review/[id]/page.tsx` is a Server Component gated by `requireSession()`, mirroring `GET /api/reviews/[id]` (Day 6) exactly — it reuses the **Day 6 read core** `getReviewForCompany`, wiring `getReviewById` through the **request-scoped anon client** (RLS is the primary tenancy guard) with the company taken from the server-resolved session and passed explicitly (never from the URL/client — Rules.md §5). It then renders the Day 9/10 hook-free `<Scorecard>` directly from the returned `PersistedReview` (the component already accepts that loose shape with the full 5-tier colour/tier/compliance visual system, so there was no visual work), plus a "Back to history" link. The page's branch is a pure module `src/lib/reviews/review-detail-view.ts` (`reviewDetailState`, mirroring the `review-form.ts`/`scorecard-view.ts` split): a 404 **or** 401 collapses to a single `not-found` state so we never reveal cross-tenant existence (Rules.md §6) — the page renders a real Next `notFound()` (honest 404) for it — while a 500 stays a distinct `error` state that surfaces a plain-language retry `role="alert"` instead of a misleading 404. New `tests/review-detail-view.test.ts` (4 tests) pins that mapping (success carries the review by reference; 401/404 → not-found; 500 → retry message); the reconstruction + 404-not-403 tenancy itself is already proven in `tests/reviews-read.test.ts`. `npm test` now **119 tests** green (14 files); typecheck clean; `npm run build` compiles (`/review/[id]` dynamic, 168 B). **Deferred (not pulled forward):** loading/error/empty-state polish across flows is Day 13; Day 27 wires the company's stored brand guide into `brand_context` (null today).

- **States & resilience (Day 13)** — Completed the loading / error / empty-state coverage across the review flows so nothing renders an unhandled state (Rules.md §6), building on the states that already existed rather than reworking them. **Loading:** new Next.js route `loading.tsx` Suspense fallbacks for the two server-rendered data pages — `/history` (a table-shaped skeleton) and `/review/[id]` (a Scorecard-shaped skeleton: Navy header + juror-card grid) — plus a reusable `src/components/ui/skeleton.tsx` primitive whose pulse is gated behind `motion-safe:` (still visible without motion — Design.md §6); the wrapping region owns `role="status"`/`aria-busy` + an sr-only label. (The submit flow's own loading — "the five jurors are reviewing…" — already lives in the `ReviewForm` result panel.) **Error:** a `(dashboard)/error.tsx` client error boundary is the backstop for any *unhandled* throw from any dashboard page — a plain-language "Something went wrong" screen with a **Try again** (`reset`) button; internal detail (name + digest) is `console.error`-logged only, never shown, no content/secrets (Rules.md §6). **Not-found:** a `(dashboard)/not-found.tsx` gives the detail page's `notFound()` (absent OR cross-tenant review — indistinguishable on purpose, Rules.md §5/§6) an on-brand 404 with links back to History / Dashboard, replacing Next's bare default. **Scorecard degradation:** a single errored juror already rendered its own "No result" card, but the scorecard now also carries a **top-level banner** when jurors are missing — a `warning` "N of M jurors couldn't be scored…" when some errored, a `danger` "None could be scored / no results" (with a retry nudge) when all did or the list is empty (which also suppresses the empty grid). The banner logic is pure and node-testable: added `summarizeJurorHealth` + `jurorHealthNotice` to `scorecard-view.ts` (banner colour→Tailwind stays at the component layer, tinted washes that clear AA and pair colour with text). `tests/scorecard-view.test.ts` grew 19→29 (health counts for healthy/partial/all-errored/empty, and the notice tone+message incl. the singular/plural and all-errored/empty cases). No new dependencies; no live-credential needs. `npm test` now **129 tests** green (14 files); typecheck clean; `npm run build` compiles. **Deferred (not pulled forward):** the app-wide accessibility AA pass is Day 62, micro-interactions/animation polish is Day 64, and structured/redacted error-monitoring hooks are Day 48 (the boundary just `console.error`s a digest for now).

- **Week 2 hardening (Day 14)** — Closed out Week 2 with a test + accessibility pass; no new product surface (mirroring the Day 7 Week-1 hardening). **Accessibility spot-check made durable:** the Design.md §6 "meaning is never colour-only" rule is now enforced by executable invariants in new `tests/accessibility-spotcheck.test.ts` (12 tests) rather than a one-off manual review — it asserts exhaustively that **every** score band (0–10) ships a paired `scoreTierLabel` word, **every** verdict (incl. not-scored) a `verdictLabel` word, **every** severity a `severityLabel`, and every confidence a spoken label; that **every** semantic `Tone` maps to a non-empty class in all four component-layer maps (`PILL_/CHIP_/BADGE_/ISSUE_BORDER_CLASSES`, with no stray keys) so a colour signal never renders style-less; that the degraded-review notice always carries message text for both tones; and that a history row derives the **same** verdict/score word + tone the scorecard would from identical data (the shared-helper contract). **Consistency fix found by the spot-check:** the `Scorecard` header meta rendered the *raw* `platform` value (e.g. "instagram") while the history table used `platformLabel` ("Instagram") — visible on `/review/[id]` and fresh submissions. `scorecard.tsx` now reuses the shared, already-tested `contentTypeLabel`/`platformLabel` from `history-view.ts` (removing a duplicated local `contentTypeLabel`), so a review's header reads identically to its history row (Design.md §1.4). No new dependencies; no live-credential needs. `npm test` now **141 tests** green (15 files); typecheck clean; `npm run build` compiles. **Deferred (not pulled forward):** the app-wide WCAG AA verification across every flow (contrast/semantics audit) is Day 62; this pass hardens the Week-2 review surface only.

#### Week 2 summary (Days 8–14) — The review web flow
Week 2 turned the Week-1 terminal/API proof into the full **submit → scorecard → history → detail** web experience, all on the established **pure view module + hook-free component** split (logic in `src/lib/reviews/*`, node-testable; colour→Tailwind isolated in `src/components/review/tone-classes.ts`). **Submission (Day 8):** the `/review` form (content + type/platform, client Zod validation mirroring the server contract, `submitReview` re-validating the response) — the API stays the trust boundary. **Scorecard + JurorCard (Day 9):** Navy-header aggregate/verdict card + per-juror cards, rendered from the loose `ReviewResult`/`PersistedReview` shape so a fresh and a stored review render identically; an errored juror shows its own "No result" state. **Verdict/score visual system (Day 10):** the full Design.md §5 5-tier score→colour, the compliance-flag Burgundy override (issue-gated), and issue-severity styling — every colour paired with a text label. **History (Day 11):** `/history` company-scoped table reusing the Day 6 read core, rows linking to detail. **Detail (Day 12):** `/review/[id]` reconstructs a stored review's scorecard, reusing the read core + `<Scorecard>` with zero visual work; a 401 collapses into the 404 so cross-tenant existence never leaks. **States & resilience (Day 13):** `loading.tsx` skeletons, a group `error.tsx` boundary, a `not-found.tsx`, and the scorecard's degraded-review banner — no unhandled states (Rules.md §6). **Hardening (Day 14):** accessibility-invariant tests + a scorecard/history platform-label consistency fix. Tenancy held throughout: every page derives the company from the server-resolved session and reads through the request-scoped anon client (RLS primary), never the client. Ends Week 2 at **141 tests green** (15 files), typecheck clean, build compiling. **Still credential-blocked (owner action):** no live model or Supabase in the cloud build, so the DB read/write paths render correctly against mocked/persisted data but are unverified against a real project; persona wording is still uncalibrated against real model output. **Next:** Week 3 — core-engine hardening (suggested-rewrite UX, configurable weighting, rate limiting, cost guardrails, provider resilience, platform-aware prompts).

- **Verdict/score visual system (Day 10)** — Formalised the Design.md §5 verdict/score visual language on the Day 9 seam (`src/lib/reviews/scorecard-view.ts` + `src/components/review/tone-classes.ts`), no new pages. **Score→colour is now the full 5 tiers** (9–10 Success · 7–8 Royal · 5–6 Warning · **3–4 Warning-deep** · 0–2 Danger): added a distinct `warning-deep` token (`#9A3412`, in `tailwind.config.ts` as `warning.deep` + a matching `--warning-deep` CSS var) and the `burgundy` compliance family to the `Tone` union, `PILL_CLASSES`/`CHIP_CLASSES`, plus new `BADGE_CLASSES` and `ISSUE_BORDER_CLASSES` maps. **Compliance-flag override (Design.md §2):** `hasComplianceFlags(juror)` is true only for a *scored compliance juror with ≥1 issue*; `jurorScoreTone(juror)` then forces the chip to Burgundy regardless of the numeric score (a clean compliance pass keeps its score band; an errored slot reads neutral), and every issue on a flagging compliance juror overrides to Burgundy via `issueTone(severity, compliance)`. **Issue-severity styling:** high→Danger, medium→Warning, low→neutral, applied to both the severity badge and the issue row's left accent border. **A11y / never colour-only (Design.md §6):** every score chip now carries a paired **tier word** (`scoreTierLabel`: Excellent/Strong/Mixed/Weak/Failing) — visible next to the chip and `sr-only` inside it — the compliance override shows a visible "Compliance flag" badge (with an `sr-only` "Risk:" prefix), and every severity badge has an `sr-only` "Severity:" prefix; all foreground colours clear WCAG AA (≥4.5:1) — white on the solid pills, same-hue text on the `/10` tinted washes. `JurorCard` was rewired to these helpers; `Scorecard`'s verdict pill already used `verdictTone`/`PILL_CLASSES` and is unchanged. `tests/scorecard-view.test.ts` grew from 8→19 (the 5-tier `scoreTone` incl. the 3–4 band, `scoreTierLabel`, `hasComplianceFlags` truth table, `jurorScoreTone` override, `issueTone`). `npm test` now **104 tests** green (12 files); typecheck clean; `npm run build` compiles (`/review` 17.4 kB). **Deferred (not pulled forward):** the app-wide a11y AA verification across every flow is Day 62; this day covers the scorecard surface.

- **Suggested-rewrite UX (Day 15)** — First Week 3 slice: made each juror's suggested rewrite **copyable** and added an expandable **before/after diff** of the rewrite vs the submitted content, on the existing scorecard seam (no new pages). Pure diff logic lives in `src/lib/reviews/rewrite-diff.ts` (node-testable like the other view modules): `computeRewriteDiff(original, rewrite)` runs an **LCS over whitespace-preserving tokens** (`tokenize` splits into whitespace/non-whitespace runs so the before/after streams reconstruct the inputs exactly) and returns `{ changed, tooLarge, segments, added, removed }` — `changed:false` when the original is missing/blank **or** the rewrite equals it (PRD §7 allows `suggested_rewrite` to match when nothing needs changing, so the comparison is simply hidden), and a `MAX_DIFF_TOKENS` (400/side) guardrail sets `tooLarge` to fall back to plain side-by-side blocks (keeps the render legible and the O(n·m) table small). `beforeSegments`/`afterSegments` filter the coalesced segments for the two views. UI: a new **client island** `src/components/review/copy-button.tsx` (`navigator.clipboard.writeText`, "Copy"→"Copied", `aria-live` status, fails quietly to "Copy failed" with the text still selectable — Rules.md §6) and `src/components/review/rewrite-panel.tsx` (hook-free wrapper embedding the copy button + a `<details>` "Compare with original" rendering the highlighted before/after). `JurorCard` now renders `<RewritePanel>` instead of the inline rewrite block and takes an `original` prop; `Scorecard` gained an `originalContent` prop threaded to every card. Both flows supply it: the detail page reads it from a new `content_text` field on `PersistedReview` (from the already-fetched `reviews` row — this is the single-review read, so returning content is intentional; the thin history `ReviewSummary` still omits it), and `ReviewForm` captures the submitted content into state on success (the API response doesn't echo it back). A11y / never colour-only (Design.md §6): added words carry an "added" cue + inserted styling, removed words a strike-through + "removed" cue, a legend, and an sr-only "adds N words, removes M words" summary. New `tests/rewrite-diff.test.ts` (11 tests: tokenize round-trips, the unchanged cases incl. rewrite-equals-original, replace/insert/delete counts, before/after reconstruction, coalescing, and the tooLarge guardrail); the Day 12 detail-view fixture gained the required `content_text`. No new dependencies. `npm test` now **152 tests** green (16 files); typecheck clean; `npm run build` compiles (`/review` 19.2 kB, `/review/[id]` 812 B). **Deferred (not pulled forward):** rewrite *usefulness* feedback (PRD §8 thumbs-up) isn't part of this day; configurable juror weights are Day 16.

- **Configurable weighting (Day 16)** — Scaffolded per-company juror weights (PRD §5.2 / §6.3) so the aggregate can honor a company's custom weighting instead of the fixed equal split; the default stays equal (0.2 each) and every existing path is unchanged. **Schema:** new `src/lib/schema/weights.ts` defines the weight config contract — `PersonaWeights` (a full 5-juror map, for the default + `computeAggregate`), `PersonaWeightsConfig` (`Partial<Record<PersonaName, number>>`, the stored/incoming shape), and `PersonaWeightsInputSchema` (a Zod validator: any subset of jurors, each a finite non-negative number, ≥1 must be > 0, unknown persona keys rejected via `.strict()` — config is untrusted input, validate before trust, Rules.md §3). **Scoring:** `src/lib/scoring.ts` renamed `PERSONA_WEIGHTS` → `DEFAULT_PERSONA_WEIGHTS`, `computeAggregate(jurors, weights = DEFAULT_PERSONA_WEIGHTS)` now takes an optional weight map (existing callers/tests pass none → identical equal-weighted behavior; error jurors are still excluded and the remaining weights renormalized), and a new pure `resolveWeights(config)` turns a stored config into a complete safe map — it starts from the equal default and overrides only jurors with a *valid* value (so a partial config leaves the rest at 0.2, and an explicit 0 disables a juror), ignoring negative/NaN/non-number values and falling back to the equal default when the resolved total is 0 (never a NaN aggregate — Rules.md §6 fail-safe). **Wiring:** `runReview(input, { client?, weights? })` threads the map into `computeAggregate`; `createReview` resolves the company's config from the **server session's** `company.juror_weights` (never the client — Rules.md §5) via `resolveWeights` and passes it in, so the aggregate honors custom weights end-to-end. **Persistence scaffold:** migration `supabase/migrations/0002_juror_weights.sql` adds a nullable `companies.juror_weights` jsonb (null = equal default, no backfill) with a coarse `jsonb_typeof = 'object'` check; `CompanyRow` gained `juror_weights: PersonaWeightsConfig | null`; and `updateCompanyJurorWeights(db, companyId, weights | null)` is the (admin-gated later) write path — explicit server-derived `companyId`, validated via `PersonaWeightsInputSchema` before the DB write, `null` clears back to default. New `tests/scoring-weights.test.ts` (22 tests): `computeAggregate` default-unchanged / custom / scale-invariant / renormalized-with-error / zero-weight cases; `resolveWeights` null/partial/disable/invalid-ignored/all-zero-fallback/no-default-mutation; the Zod schema's accept/reject cases; the write helper's validate-then-scope behavior; and an end-to-end `createReview` check that a heavily-weighted juror shifts the aggregate (6.0 equal → 8.0 weighted) using a fixed-score fake client. Three `CompanyRow` test fixtures gained `juror_weights: null`. No new dependencies; no live-credential needs. `npm test` now **174 tests** green (17 files); typecheck clean; `npm run build` compiles. **Deferred (not pulled forward):** the admin UI to *edit* weights (a brand/settings surface calling `updateCompanyJurorWeights`, PRD §6.3 "custom juror weights per company") — Day 16 scaffolds the storage, contract, and read path only.

- **Rate limiting (Day 17)** — Per-plan review rate limits enforced in `POST /api/reviews`, so an unbounded submit rate can't blow the AI budget (Rules.md §1 cost discipline; each review fans out to five model calls). **Policy + decision logic** lives in a new pure module `src/lib/rate-limit.ts` (node-testable like the other lib modules): `PLAN_RATE_LIMITS` (a rolling **24h window** — free **10**, pro **100**, enterprise **1000**; a first-pass sizing, tunable), `resolveRateLimitPolicy(plan)` (falls back to the most restrictive `free` for an unknown/missing tier — fail closed on plan value), `rateLimitWindowStart(policy, now)`, and the pure `evaluateRateLimit(policy, usage, now)` → `{ allowed, limit, remaining, resetAt, retryAfterSeconds }`. It's a rolling window: the new review isn't counted yet, so `count < limit` allows it (a company at `count === limit` is blocked); the reset is when the **oldest** counted review ages out of the window (`oldest + windowMs`), giving an honest `Retry-After`. `rateLimitMessage` renders the plain-language 429 (names the limit + when to retry + upgrade nudge — Rules.md §6). **DB read:** new `getReviewRateUsage(db, companyId, sinceIso)` in `queries.ts` returns `{ count, oldest }` in a single exact-count query (`select('created_at', { count: 'exact' }).gte(...).order(asc).limit(1)`) so only one row crosses the wire regardless of the count; the existing `reviews(company_id, created_at desc)` index covers it (no migration). **Wiring:** `createReview` gained an optional `getRateUsage` dep and enforces the limit **before** the five-juror fan-out (no model spend on a blocked request), keyed to the plan on the **server-resolved** company row (never the client — Rules.md §5); a 429 result carries the `RateLimitDecision`. A usage-read failure **fails open** (rate limiting is a soft cost guard, not an authz boundary) and is logged with redacted context. The route wires `getRateUsage` to `getReviewRateUsage` via the request-scoped anon client (RLS-scoped) and, on 429, sets `X-RateLimit-Limit`/`-Remaining`/`-Reset` + `Retry-After` headers and echoes the reset info in the JSON body. New `tests/rate-limit.test.ts` (21 tests): the pure policy/decision/message logic (allow/block/at-limit boundary/reset math/plan fallback/floor-safety), `createReview` enforcement (429-with-no-persist over limit, correct company_id + window in the usage query, allow-under-limit, fail-open on read error, back-compat when no reader is injected), and the `getReviewRateUsage` helper (company_id + window filter, count+oldest, zero-usage, error-with-context). No new dependencies; no live-credential needs. `npm test` now **195 tests** green (18 files); typecheck clean; `npm run build` compiles. **Deferred (not pulled forward):** content-size/token-budget cost guardrails + redacted usage logging are Day 18; a shared rate-limit surface for exports is Day 40.

- **Cost guardrails (Day 18)** — Content size limits + a per-review token budget enforced before the five-juror fan-out, plus redacted usage logging (Architecture.md §5 "token budget guardrails"; Rules.md §1 cost discipline, §3 no-PII/secret logging, §6 error handling). **New pure module `src/lib/ai/cost.ts`** (node-testable like the other lib modules) owns two layers and every constant: `MAX_CONTENT_CHARS` (10k — now the **single source of truth** for the size cap), `MAX_OUTPUT_TOKENS_PER_JUROR` (1024), `PROMPT_OVERHEAD_TOKENS_PER_JUROR` (400), `MAX_REVIEW_INPUT_TOKENS` (20k), and a `~4-chars-per-token` `estimateTokens` heuristic (estimates only — for budgeting/observability, never billed). **Layer 1 — size + budget guard:** `checkContentSize(content)` trims then rejects empty (`empty`) or over-cap (`too_long`) content with a specific, plain-language message + limit/actual (Rules.md §6); `checkTokenBudget({contentTokens, jurorCount, brandContextTokens})` rejects a fan-out whose estimated **total input tokens** (content + per-juror overhead + brand context, × jurors) exceed `MAX_REVIEW_INPUT_TOKENS` with a generic user message (internal token numbers stay in the decision, not the copy). The budget is sized with clear headroom above a max-length five-juror review so it **never contradicts** the advertised 10k limit — it exists to catch a genuinely oversized fan-out once brand context is threaded in (Day 27) or the juror set grows. **Layer 2 — redacted usage log:** `estimateReviewUsage(content, jurorCount)` → `{jurorCount, contentTokens, estimatedInputTokens, maxOutputTokens}`; `logReviewUsage(usage, meta)` emits one `console.info("review usage", …)` line whose record (`buildUsageLogRecord`, pure so the redaction is unit-testable) carries **only** IDs, counts, and derived scores — the `ReviewUsageLogMeta` type makes "no content" a compile-time property (there is no field to put content in). **Wiring:** `createReview` runs both guards right after schema validation and **before** the rate-limit read + fan-out (no model spend on a rejected request), with `jurorCount = PERSONA_NAMES.length` and `brandContextTokens: 0` (brand context is Day 27); on a successful persist it logs redacted usage, deriving `isMock` from the composed `review.model`'s `(mock)` suffix (accurate whether or not a client was injected). **Single source of truth threaded out:** `ReviewInputSchema.content_text.max()` and the submission form's `MAX_CONTENT_LENGTH` counter now both derive from `MAX_CONTENT_CHARS`, and `client.ts` caps the Anthropic `max_tokens` at `MAX_OUTPUT_TOKENS_PER_JUROR`, so the counter, client validation, server cap, and output ceiling can't drift. New `tests/cost-guardrails.test.ts` (15 tests: token estimate rounding, size guard accept/empty/at-cap/over-cap/whitespace, input-token scaling + negative-flooring, budget allow/at-limit/over-budget, usage assembly, and the **redaction contract** — the log record never contains the content or a would-be secret and has exactly the safe key set); `tests/reviews-endpoint.test.ts` gained an integration test proving a successful `createReview` logs redacted usage with no content (now 11). The endpoint/rate-limit/weights suites silence the new `console.info` line in a `beforeEach` to keep output clean. No new dependencies; no live-credential needs. `npm test` now **211 tests** green (19 files); typecheck clean; `npm run demo` and `npm run build` compile. **Deferred (not pulled forward):** per-plan token budgets (the budget is global for now), and brand-context token accounting lights up when Day 27 threads the stored guide into `brand_context` (0 today); provider timeout/backoff resilience is Day 19.

- **Provider resilience (Day 19)** — Timeout + backoff on the model calls, so a hung connection or a transient provider blip degrades one juror gracefully instead of stalling or failing the whole review (Rules.md §6 — "AI provider timeout / rate limit → fail the affected juror(s) gracefully; respect provider backoff; surface a retryable error"; Architecture.md §5 provider abstraction). **New pure module `src/lib/ai/resilience.ts`** (node-testable like the other lib modules) owns two provider-facing concerns: (1) a per-attempt **timeout** — `withTimeout(fn, ms)` creates an `AbortController`, races the call against a timer, and on expiry **aborts the signal** (so the real request is cancelled, not just abandoned) and throws a `TimeoutError`; (2) **exponential backoff with jitter** — `computeBackoffMs(attempt, opts)` (`base × factor^attempt`, capped at `maxDelayMs`, optional full jitter via an injectable `random`) and `callWithResilience(fn, opts)` which retries **only** transient failures and never a permanent one. `classifyModelError` maps a failure to `timeout | rate_limit | network | permanent`: a `TimeoutError`/`AbortError` → timeout, HTTP **429** → rate_limit, **5xx** → network (all retryable), a non-429 **4xx** → permanent (a bad request/invalid key — retrying would just burn spend), and an unclassifiable error defaults to retryable `network` (a bounded retry is cheaper than failing fast on a recoverable blip). Timers and randomness are **injectable** so the retry/backoff math is deterministic under test. **Client seam:** `CompleteArgs` gained an optional `signal?: AbortSignal`; `AnthropicClient.complete` now passes it as the SDK request option (`messages.create(body, { signal })`) so the timeout genuinely cancels the in-flight call, and the offline `MockClient` honours an already-aborted signal (same cancellation contract). **Orchestrator wiring:** `runPersona` wraps the model call in `callWithResilience` (`DEFAULT_RESILIENCE`: 20s per-call timeout, up to 3 attempts, 300ms base × 2 backoff capped at 4s, jitter on) and keeps the two resilience layers **distinct** — the provider timeout/backoff retry is separate from the existing Rules.md §6 "invalid JSON → retry once with a corrective nudge" contract, and the corrective nudge is now sent **only** when the previous failure was invalid *output* (a nudge can't fix a dead connection), tracked via `isOutputError` (ZodError/SyntaxError). A juror that exhausts its retries degrades to an `error` slot carrying a concise, content-free `jurorErrorReason` (e.g. "provider timeout", "invalid model output (schema mismatch)") that stays **internal** (logs + the slot's `error` field); the UI keeps showing its own generic message. `runReview` gained an optional `resilience?: Partial<ResilienceOptions>` dep threaded into every `runPersona` (defaults to production values; tests inject a no-wait policy). **UI:** the `JurorCard` "No result" error card copy was generalised so it's accurate for a timeout/provider failure too ("couldn't be scored — it may have timed out or returned an unusable result … try submitting again"), still paired with the existing Day 13 scorecard degraded-review banner ("N of M jurors couldn't be scored"). New `tests/provider-resilience.test.ts` (22 tests): `classifyModelError` (timeout/429/5xx/4xx/AbortError/default), `computeBackoffMs` (exponential/cap/jitter/non-negative), `withTimeout` (fast resolve, hung-call abort+reject, pass-through of the call's own rejection), `callWithResilience` (first-try success, transient-retry-then-succeed with growing backoff, no-retry-on-permanent, exhaust-then-throw, real-timeout-retry), and the **DoD end-to-end**: with a fake client that fails one persona's provider, `runReview` still returns a schema-valid review with that juror `error` and the other four `ok` (aggregate from the healthy jurors); an all-persona provider failure returns five `error` slots and aggregate 0 (which `createReview` turns into a 502, proven in the endpoint suite) without the orchestrator ever throwing. No new dependencies; no live-credential needs (the offline mock still drives everything). `npm test` now **233 tests** green (20 files); typecheck clean; `npm run demo` and `npm run build` compile. **Deferred (not pulled forward):** platform-aware persona prompts are Day 20; per-plan/adaptive timeout tuning and a circuit-breaker across reviews aren't needed at this scale; structured error-monitoring hooks (beyond the redacted `console.warn`/`console.error` retry+failure logs) are Day 48.

- **Platform-aware prompts (Day 20)** — The SEO / Discoverability and "Would I Stop Scrolling" jurors now adapt their expectations to the selected target platform, so a Google search ad, an Instagram caption, and a newsletter subject line are judged by the conventions that actually apply to each (DailyPlan Day 20; PRD §5 jurors 4 & 5). **New pure module `src/lib/ai/personas/platform-guidance.ts`** (node-testable like the other lib modules) owns the per-platform, per-persona guidance and lives under `personas/` because it is prompt content and prompt content is reviewed code (Rules.md §3): `PLATFORM_AWARE_PERSONAS` (exactly `seo_discoverability` + `stop_scrolling` — the other three jurors' lenses don't shift by channel, so scoping stays tight), `PLATFORM_GUIDANCE` (curated guidance for both jurors keyed by a normalized platform slug — google, facebook, instagram, linkedin, x, tiktok, youtube, email, website, mirroring the submission form's `PLATFORM_OPTIONS`), `normalizePlatform` (trims/lowercases + folds common aliases like `twitter`→`x`, `newsletter`→`email`, `blog`→`website`; `null`/empty → no platform), and the pure `platformGuidanceFor(persona, platform)` → the curated block for a known platform, a **generic platform-naming nudge** for an unrecognized-but-named platform (so the prompt still reflects it), or `null` for a non-platform-aware persona / no platform. **Orchestrator wiring:** `buildUserPrompt` (now exported for testing) injects a "Platform-specific expectations for this juror (target platform: …)" block into the two platform-aware jurors' user prompts when guidance exists; other jurors' prompts and the null-platform case are byte-for-byte unchanged (the generic `Platform:` context line already existed). **System prompts:** SEO already said "Tailor expectations to the given platform"; the stop-scrolling role gained a sentence telling it to calibrate stopping power to the platform, so both platform-aware jurors reflect it at the system-prompt level too. New `tests/platform-guidance.test.ts` (18 tests): the platform-aware set, structural completeness of `PLATFORM_GUIDANCE` (both persona keys non-empty, every curated form option covered), `normalizePlatform` (null/empty/whitespace, trim+lowercase, aliases, unknown pass-through), `platformGuidanceFor` (null for non-aware personas / no platform, curated known-platform guidance, the two jurors differ for the same platform, alias/case normalization, the generic unknown-platform nudge), the **DoD end-to-end** via `buildUserPrompt` (platform injected for SEO/stop-scrolling, absent for a non-aware juror, absent when no platform is selected), and that both jurors' system prompts mention the platform. No new dependencies; no live-credential needs (the offline mock still drives everything). `npm test` now **251 tests** green (21 files); typecheck clean; `npm run demo` and `npm run build` compile. **Deferred (not pulled forward):** Week 3 hardening (tests + calibration + Memory refresh + Week 3 summary) is Day 21; calibrating the platform wording against real model output still waits on a live key; extending platform-awareness to other jurors isn't wanted (their lenses are channel-independent).

- **Week 3 hardening (Day 21)** — Closed out Week 3 with a test + calibration pass; no new product surface (mirroring the Day 7 and Day 14 week-end hardening). **Calibration:** a new shared `REWRITE_GUIDANCE` fragment in `src/lib/ai/personas/base.ts`, composed into **every** persona system prompt via `composeSystemPrompt` (like the Day 7 `CONFIDENCE_GUIDANCE`). The `OUTPUT_CONTRACT` already *required* a `suggested_rewrite` but gave the model no guardrails on what it may contain, so a rewrite was free to invent facts — a real risk given the compliance juror could "fix" a claim by inventing substantiation or a disclaimer (adding new risk rather than removing it), and any juror could add numbers/prices/features never in the original. The fragment keeps every rewrite truthful to the submitted content and scoped to the juror's own lens (Rules.md §3 — the AI critiques, it does not fabricate), and prefers softening/removing a risky claim over inventing support. `tests/personas.test.ts` now pins it (embedded in all five prompts, + a content assertion that it forbids invented claims/disclaimers). **Orchestrator integration suite (new `tests/orchestrator.test.ts`, 6 tests):** Week 3 layered three behaviours onto the fan-out that were only unit-tested in isolation (Day 20 `buildUserPrompt`) or asserted via comments (Day 19); this drives the *whole* `runReview` path with a recording model client that captures every `client.complete` call, proving end-to-end: platform guidance reaches **only** the SEO + stop-scrolling user prompts (and no juror when no platform is selected), the recorded prompt matches `buildUserPrompt`, every juror's system prompt carries the shared calibration fragments, the corrective JSON nudge is appended **only** on the retry after an *output* error and **never** after a transient provider failure (a nudge can't fix a dead connection — the Day 19 invariant, previously untested), a juror degrades to a clean content-free `error` slot after its output retries are exhausted while the other four still return, and the persona field is force-set to the requested juror even when the model echoes a different valid persona name. **Verdict coverage (expanded `tests/schema.test.ts`, +5):** the Phase 2 acceptance criterion ("scoring/verdict logic covering pass/revise/fail and the compliance veto") had gaps — added a compliance `score <= 2` veto with no high-severity issue (the low-score veto branch), the exact `<= 2` boundary (score 3 with no flag does **not** veto → pass), an errored compliance juror does **not** veto (the veto needs an ok compliance result; verdict falls through to the aggregate), a low aggregate (< 5.0) fails with no compliance flag, and — a multi-tenant-safety invariant — the compliance veto **cannot be weighted away** (a company setting `compliance_legal_flagger` weight to 0 drops it from the weighted mean but the veto still fails the verdict, because the veto is a risk gate on the verdict, not a term in the aggregate — Day 16). No new dependencies; no live-credential needs. `npm test` now **264 tests** green (22 files); typecheck clean; `npm run demo` and `npm run build` compile. **Deferred (not pulled forward):** calibrating prompt wording against real model output still waits on a live key; the admin UI to edit juror weights (calling `updateCompanyJurorWeights`, PRD §6.3) and wiring the stored brand guide into `brand_context` (Day 27) are Week 4+.

#### Week 3 summary (Days 15–21) — Core-engine hardening
Week 3 hardened the Week-2 review flow into a cost-disciplined, resilient, platform-aware engine, all on the established **pure lib module + injectable orchestrator** pattern (`scoring.ts`, `rate-limit.ts`, `ai/cost.ts`, `ai/resilience.ts`, `ai/personas/platform-guidance.ts`), so every new behaviour is node-testable without a live model. **Rewrite UX (Day 15):** each juror's `suggested_rewrite` is copyable with an expandable LCS-based before/after diff of the rewrite vs the submitted content. **Configurable weighting (Day 16):** scoring reads a per-company weight config resolved safely onto the equal default (partial/invalid/all-zero → safe fallback), threaded from the server session's company row; storage column + validated write helper scaffolded (edit UI deferred). **Rate limiting (Day 17):** per-plan rolling-window review limits enforced in `createReview` **before** the five-call fan-out (429 + honest reset/`Retry-After`), plan taken from the server-resolved company, fails open on a read blip. **Cost guardrails (Day 18):** a single-source-of-truth content size cap + a per-review token budget enforced before the fan-out, plus a compile-time-redacted usage log (no field exists to hold content/secrets). **Provider resilience (Day 19):** a per-call timeout (via an `AbortSignal` that genuinely cancels the request) + exponential backoff that retries only transient failures, kept distinct from the invalid-JSON corrective nudge; a stuck juror degrades to a clean `error` slot. **Platform-aware prompts (Day 20):** the SEO + stop-scrolling jurors get per-platform expectations injected into their user prompts (aliases folded, generic nudge for unknown platforms), other jurors unchanged. **Hardening (Day 21):** a shared no-fabrication rewrite-calibration fragment across all five prompts, an orchestrator integration suite pinning the Day 19/20 invariants end-to-end, and full compliance-veto verdict coverage (incl. the veto being independent of per-company weights). Cost discipline held throughout — one model, five prompts, guards before spend — and multi-tenancy held: every path derives the company from the server-resolved session, never the client. Ends Week 3 at **264 tests green** (22 files), typecheck clean, demo + build compiling. **Still credential-blocked (owner action):** no live model or Supabase in the cloud build, so the DB read/write and auth paths are code-complete but unverified against a real project, and persona/platform/rewrite wording is uncalibrated against real model output. **Next:** Week 4 — multi-tenant foundations (company onboarding, team management, invitations, role management, brand-profile CRUD, brand voice in reviews).

- **Company onboarding (Day 22)** — First Week 4 slice: a **first-admin onboarding flow** that turns the name-only company auto-provisioned at signup (Day 3) into a described, usable company context — the admin names/renames it and sets its **industry + plan tier** (DailyPlan Day 22; PRD §4 admin buyer). **Signal:** migration `supabase/migrations/0003_company_onboarding.sql` adds a nullable `companies.onboarded_at timestamptz` (null = not yet onboarded, no backfill — existing rows read as needs-onboarding); `CompanyRow` gained `onboarded_at: string | null`, and the coarse "explicit per-company signal" pattern mirrors Day 16's `juror_weights`. **Pure module `src/lib/company/onboarding.ts`** (node-testable) owns the contract + helpers: `INDUSTRY_OPTIONS` (a curated `value`/`label` list stored in `companies.industry`, incl. an `other` catch-all — `value` is what persists), `PLAN_TIER_OPTIONS` (free/pro/enterprise with taglines), `CompanyOnboardingSchema` (Zod: name trim 2–120, `industry` ∈ the curated set, `plan_tier` ∈ the three tiers — validate before trust, Rules.md §3), `needsOnboarding(company)` (the pure `onboarded_at == null` predicate the route gate + tests share), and display helpers (`industryLabel`, `planTierLabel`, `planReviewAllowance` — the last reads `PLAN_RATE_LIMITS` so the UI's "N reviews/day" can't drift from Day 17's limits). **Write path:** `completeCompanyOnboarding(db, companyId, {name, industry, plan_tier}, now?)` in `queries.ts` sets the three fields + stamps `onboarded_at`, scoped to a **server-derived** `companyId` (never the client — Rules.md §5), `now` injectable for deterministic tests; it runs under the **service-role client** because `companies` has no client write policy (writes are server-side, admin-gated — same as the Day 16 weights write path). **Server action** `src/lib/company/actions.ts` `completeOnboardingAction`: `requireAdmin()` re-derives the caller server-side (an anon/member caller is redirected, so onboarding can't be completed by a non-admin), re-validates with the schema, writes with the service-role client, and `redirect("/dashboard")` on success; a write failure returns a plain message and logs redacted context (Rules.md §6). **UI:** a standalone route `src/app/onboarding/page.tsx` (outside the `(dashboard)` group so the gate can't loop) with an auth-style centered layout, gated by `requireAdmin()`, prefilling the form from the resolved company row; the client form `src/components/company/onboarding-form.tsx` (name input, industry `<select>`, plan-tier **radio group in a `<fieldset>`** showing each tier's allowance) reuses the shared `SubmitButton`/`FormError` primitives. **Gate:** `(dashboard)/layout.tsx` now redirects an **admin** whose company `needsOnboarding` to `/onboarding` (members are never redirected — the app still works with the signup name; their admin onboards the company). Accessibility (Design.md §6): real `<label>`/`<legend>`s, keyboard-navigable radios, visible focus rings, `role="alert"` errors. Plan tier here is a **scaffold selection, not a paid entitlement** (billing is Week 11) — it drives the Day 17 review rate limits today. `seed.sql` marks the dev company onboarded. New `tests/company-onboarding.test.ts` (14 tests: `needsOnboarding`, option-list structure, label/allowance helpers incl. the rate-limit single-source check, the schema's accept/trim/reject cases, and the write helper's validated-fields/server-scoped-id/stamped-`onboarded_at`/error-with-context behavior); five existing `CompanyRow` fixtures gained `onboarded_at`. No new dependencies; no live-credential needs (the pure logic + write helper test offline). `npm test` now **278 tests** green (23 files); typecheck clean; `npm run demo` and `npm run build` compile (`/onboarding` dynamic, 2.55 kB). **Deferred (not pulled forward):** team management UI is Day 23, invitations Day 24, role management Day 25, brand-profile CRUD Day 26, brand voice in reviews Day 27; a full company **settings** surface to edit these post-onboarding is Day 50 (onboarding is reachable by direct URL to re-edit for now); wiring plan tier to real billing is Week 11.

- **Team management UI (Day 23)** — Second Week 4 slice: an **admin-only `/team` page** that lists the company's members and their roles (DailyPlan Day 23; PRD §6.1 auth + roles). The `listUsersByCompany` query already existed (Day 2), so this day is the read-and-render surface on top of it, built on the established **pure view module + injectable read core + hook-free component + server page** split. **Pure module `src/lib/company/team-view.ts`** (node-testable): `roleLabel` (admin/member → "Admin"/"Member"), `formatJoinedDate` (reuses Day 11's deterministic UTC-parts `formatHistoryDate` so a joined date reads identically to a review date — Design.md §1.4), `toTeamMember`/`toTeamRoster` (project `UserRow`s into a view-model, ordered **admins first then earliest-joined** as a pure function of the data, not query order; marks the signed-in admin's own row via `isCurrentUser`; `toTeamRoster` copies before sorting so it never mutates its input), and `summarizeTeam` (total/admins/members counts for the header). **Read core `src/lib/company/read-team.ts`** (`getTeamForCompany`, injectable `list` + session, mirroring `read-reviews.ts`): 401 when unauthenticated, **403 when the caller isn't an admin** (team management is admin-only — enforced on the server, not just hidden in the UI; fail closed, Rules.md §5/§6), 200 with the members otherwise, 500 on a query failure (redacted log + plain message). The company is taken from the session and passed explicitly to the query (never the client — Rules.md §5). **Component `src/components/company/team-list.tsx`** (hook-free): a quiet bordered/zebra table (Design.md §5 "Tables/history", mirroring `HistoryList`) — Member (email + a "You" badge for the current user), Role (badge: navy for admin, neutral for member), Joined — plus a `TeamSummaryLine`. Colour never travels alone (Design.md §6): the role badge carries its word (with an `sr-only` "Role:" prefix), real `<table>` semantics (`sr-only <caption>`, `<th scope="col">`). **Page `src/app/(dashboard)/team/page.tsx`** (replaces the Day 4 placeholder): `requireAdmin()` gates it, then wires `getTeamForCompany` to `listUsersByCompany` through the **request-scoped anon client** (RLS primary), rendering the table + summary or a `role="alert"` on failure. New `tests/company-team.test.ts` (13 tests: `roleLabel`, `formatJoinedDate` incl. the invalid case, `toTeamMember` current-user marking, `toTeamRoster` admins-first/earliest-joined ordering + no-mutation, `summarizeTeam` counts incl. empty, and the read core's 200-scoped-to-session-company / 401-no-session / **403-member-never-queries** / 500-redacted cases). No new dependencies; no live-credential needs (the pure logic + injectable core test offline). `npm test` now **291 tests** green (24 files); typecheck clean; `npm run build` compiles (`/team` dynamic, 143 B). **Deferred (not pulled forward):** inviting members is Day 24, role promote/demote (server-enforced) is Day 25, brand-profile CRUD Day 26, brand voice in reviews Day 27; a full company **settings** surface is Day 50.

- **Invitations (Day 24)** — Third Week 4 slice: an **invite-by-email token flow** that lets an admin add a teammate to the **existing** company, closing the DoD "an invite token adds a member to the right company" (DailyPlan Day 24; PRD §6.1/§6.2). **Schema:** migration `supabase/migrations/0004_invitations.sql` adds an `invitations` table (company_id, email, role, `token_hash`, status pending/accepted/revoked, invited_by, expires_at, accepted_at) with RLS enabling **admin-only SELECT** of the company's own invites and **no client write policy** (writes are server-side/service-role, admin-gated — the Day 16/22 pattern); `InvitationRow` + `InvitationStatus` added to `types/db.ts`. **Security (Rules.md §4):** the raw token is a **bearer credential** placed only in the emailed link and **never stored** — we persist a SHA-256 hash and match by hashing the presented token, so a DB leak can't hand over a usable invite. **Pure module `src/lib/company/invitations.ts`** (node-testable): token primitives (`generateInviteToken` with an injectable RNG, `hashInviteToken`, `inviteExpiresAt` = +7 days, `inviteAcceptPath`/`inviteAcceptUrl`), the `CreateInvitationSchema` (email trim/lowercase + role, validate before trust — Rules.md §3), the expiry-aware `invitationState`/`isInvitationAcceptable`/`invitationEmailMatches` predicates (unparseable expiry → expired, fail closed), and the display helpers (`toInvitationView`/`toInvitationList` — actionable invites first, newest within a group; never surfaces the hash). **DB helpers** in `queries.ts`: `insertInvitation`, `getInvitationByTokenHash`, `listInvitationsByCompany`, `markInvitationAccepted`, and `findCompanyMemberByEmail` (case-insensitive `ilike`, company-scoped) to block inviting an existing member. **Three injectable cores** (mirroring `read-team.ts`): `create-invitation.ts` (401 unauth / 403 non-admin / 400 bad email / 409 already-a-member / 201 with the raw token returned **once** for the link; company_id + invited_by from the **session**, never the client — Rules.md §5), `accept-invitation.ts` (the DoD — 401 no auth, 404 unknown token, 410 expired/used/revoked, **403 email-mismatch**, 409 already-belongs-to-a-company, else insert the member into the **invite's** company with the invite's role and mark accepted; the mark is a bookkeeping follow-up that logs-but-succeeds if it fails, since the member already exists), and `read-invitations.ts` (admin-only pending list for the team page). **Server actions** (`company/actions.ts`): `createInvitationAction` (`requireAdmin`, service-role write, **stubbed email = `console.info` the link + return it for the admin to copy**, `revalidatePath("/team")`), `acceptInvitationAction` (signed-in one-click accept; resolves the auth user directly since they're usually unprovisioned), and `signUpViaInviteAction` (a **new** invitee sets a password → account is created **for the invitation's email** and joined into the inviting company via the shared accept core — crucially **not** a new company, unlike normal signup). **UI:** the `/team` page gained an `InviteForm` (email + role, shows the shareable link on success) and an `InvitationList` (status-badged table, colour never alone — Design.md §6); a standalone `src/app/invite/[token]/page.tsx` (outside `(dashboard)` so an unprovisioned/anon invitee isn't gated) resolves the invite by token hash and branches to accept-vs-signup-vs-message; `NEXT_PUBLIC_APP_URL` added to `.env.example` for absolute links (optional, falls back to a relative path). New `tests/company-invitations.test.ts` (33 tests: token/hash determinism + injectable RNG, schema, lifecycle predicates, view ordering, and the create/accept/read cores incl. every authz/tenancy/expiry/email-match branch and the DoD "joins the invite's company with the invite's role") and `tests/invitations-queries.test.ts` (5 tests: a fake PostgREST builder proves each query applies its tenancy filter and stores the hash/pending status, never a raw token). No new dependencies (SHA-256/CSPRNG via Node's `crypto`); no live-credential needs (the pure logic + injectable cores test offline). `npm test` now **332 tests** green (26 files); typecheck clean; `npm run demo` and `npm run build` compile (`/invite/[token]` 1.61 kB, `/team` 2.21 kB). **Deferred (not pulled forward):** role promote/demote is Day 25, brand-profile CRUD Day 26, brand voice in reviews Day 27; revoking/resending an invite and a real email transport are follow-ups (the link is logged + shown for now); the single-company-per-user model means an existing member can't be re-invited to a second company (409 by design).

- **Role management (Day 25)** — Fourth Week 4 slice: an **admin promotes or demotes a teammate** from the `/team` table, authorized on the server (DailyPlan Day 25; PRD §6.1 auth + roles). Built on the established **pure module + injectable core + client island + service-role write** split. **Pure module `src/lib/company/role-management.ts`** (node-testable): the `RoleChangeSchema` (Zod — `userId` trimmed/non-empty + `role` ∈ admin|member, validate before trust — Rules.md §3), `firstRoleChangeIssue`, the safety predicate `wouldRemoveLastAdmin(members, targetUserId, newRole)` (true **only** when the target is the sole admin being demoted — the one change that must be blocked so a company can't lock itself out of admin management, Rules.md §5/§6 fail closed), and the toggle display helpers `nextRole`/`roleActionLabel` (member → "Make admin", admin → "Make member"). **Injectable core `src/lib/company/update-role.ts`** (`changeMemberRole`, mirroring `create-invitation.ts`): 401 unauth / **403 non-admin** (admin-only, enforced on the server not just hidden in the UI — never reads or writes for a member) / 400 bad input / **404 target-not-on-team** (the target is resolved from the company's own session-scoped roster, so a stranger or another company's user is indistinguishable from absent — never reveal cross-tenant existence, Rules.md §6) / idempotent **200 no-op** when the target already has the role (no write) / **409 last-admin** demotion / 200 with the updated row otherwise; a roster-read or write failure maps to 500 with a redacted log + plain message. Tenancy is server-derived: the company id comes from the session and is passed to both the roster read and the write, never the client (Rules.md §5). **DB write** `updateUserRole(db, companyId, userId, role)` in `queries.ts` scopes the update by **both `id` and `company_id`** (so a service-role write can't touch another company's user even if handed a foreign id) and runs under the service-role client (`users` has no client write policy — the Day 16/22/24 pattern). **Server action** `changeMemberRoleAction` (`company/actions.ts`): `requireAdmin()` re-derives the caller, the core does the work, `revalidatePath("/team")` refreshes the table, and it returns a plain confirmation/notice. **UI:** a new client island `src/components/company/role-actions.tsx` renders the per-row promote/demote toggle (Navy-outline secondary button per Design.md §5, its own `useActionState` so each row's error/notice is scoped, `useFormStatus` "Saving…" pending state); `team-list.tsx` gained a Manage column wiring it per row and disables the control for the **sole admin** ("Only admin", with an sr-only explanation) rather than letting a click bounce off the 409. Accessibility (Design.md §6): the button carries an `aria-label` naming the action + target, outcomes are announced (`role="alert"`/`role="status"`), the new column header is sr-only. New `tests/company-role-management.test.ts` (23 tests: the schema, `wouldRemoveLastAdmin` truth table, the toggle helpers, and the core's promote/demote/401/403/400/404/no-op/409-last-admin/500 branches incl. the server-derived-company-id tenancy assertion) and `tests/user-role-query.test.ts` (2 tests: a fake PostgREST builder proves the write scopes by id **and** company_id and throws with redacted context on error). No new dependencies; no live-credential needs (pure logic + injectable core + fake-builder test offline). `npm test` now **354 tests** green (28 files); typecheck clean; `npm run demo` and `npm run build` compile (`/team` 2.66 kB). **Deferred (not pulled forward):** brand-profile CRUD is Day 26, brand voice in reviews Day 27; removing a member and re-inviting/revoking invites are follow-ups; a full company **settings** surface is Day 50. **Design decision:** self-demotion is *allowed* when another admin exists (an admin can step down) — the last-admin guard, not a blanket self-change block, is the invariant that prevents lockout.

- **Brand profile CRUD (Day 26)** — Fifth Week 4 slice: an **admin-only `/brand` editor** to create/edit the company's tone/style guide, which Juror 1 (Brand Voice Guardian) reviews content against (DailyPlan Day 26; PRD §5 juror 1, §6.1). Built on the established **pure module + injectable core + client island + service-role/RLS write** split. **Pure module `src/lib/company/brand-profile.ts`** (node-testable): the `BrandProfileSchema` (Zod — `tone_guide_text` trimmed, `MIN_TONE_GUIDE_CHARS` 10 / `MAX_TONE_GUIDE_CHARS` 8000; validate before trust — Rules.md §3), `firstBrandProfileIssue`, and the display predicates/helpers `hasBrandProfile` (a null/whitespace-only guide reads as "not set"), `toneGuideText` (the text or "" for prefill, never null), and `brandProfileUpdatedAt` (reuses Day 11's `formatHistoryDate` so the "last updated" date reads identically app-wide — Design.md §1.4; invalid/absent → null). **Injectable core `src/lib/company/save-brand-profile.ts`** (`saveBrandProfile`, mirroring `update-role.ts`): 401 unauth / **403 non-admin** (admin-only, enforced on the server not just hidden in the UI — never reads or writes for a member) / 400 bad input / else **create-or-edit** — it reads the company's current profile and, if one exists, **updates it in place** (scoped by its id AND company_id) else **inserts** the first one, returning `{ created }`; a read or write failure maps to 500 with a redacted log + plain message (the guide text is never logged — Rules.md §6). Tenancy is server-derived: the company id comes from the session and is passed to the read and both writes, never the client (Rules.md §5). **DB write** `updateBrandProfile(db, id, companyId, patch)` in `queries.ts` scopes the update by **both `id` and `company_id`** (so a write can't touch another company's row even if handed a foreign id — the Day 25 pattern) and bumps `updated_at` explicitly (an UPDATE doesn't re-run the column default); `embedding_ref` is left untouched (the brand-voice cache is Days 29–30). `insertBrandProfile` (from Day 2) is reused for the first-guide case. **Server action** `saveBrandProfileAction` (`company/actions.ts`): `requireAdmin()` re-derives the caller, the core does the work, `revalidatePath("/brand")` refreshes the page, and it returns a plain created/updated confirmation. **Write path decision:** unlike the other company writes (companies/users/invitations have **no** client write policy, so they use the service role), `brand_profiles` has an **admin-only RLS write policy** — so this write goes through the **request-scoped client** and RLS is the DB-level guard, directly satisfying the DoD "admin-only via RLS"; the core's 403 + the page `requireAdmin()` are defense-in-depth. **UI:** the Day 4 placeholder `src/app/(dashboard)/brand/page.tsx` is replaced with a real page (`requireAdmin()` gate, reads the current profile via `getBrandProfileByCompany` through the request-scoped client so `brand_profiles_select` RLS is the read guard, renders a "Guide set / Not set yet" status pill + last-updated line, and a `role="alert"` on a load failure) and a client island `src/components/company/brand-profile-form.tsx` (a labelled textarea with a live character counter wired via `aria-describedby`, `useActionState`, and the shared `FormError`/`FormNotice`/`SubmitButton` primitives). Accessibility (Design.md §6): a real `<label>`, help text and the counter linked by `aria-describedby`, outcomes announced via `role="alert"`/`role="status"`, Design.md tokens throughout. New `tests/company-brand-profile.test.ts` (16 tests: the schema's accept/trim/empty/min/at-max/over-max cases, the display helpers, and the core's 401/403/400/insert-created/update-in-place/500-read/500-write branches incl. the server-derived-company-id tenancy assertions and the no-content-in-logs redaction check) and `tests/brand-profile-query.test.ts` (3 tests: a fake PostgREST builder proves `updateBrandProfile` scopes by id AND company_id + bumps `updated_at` + leaves `embedding_ref` untouched, `insertBrandProfile` stores the company-scoped row, and the error path throws with redacted context). No new dependencies; no live-credential needs (pure logic + injectable core + fake-builder test offline). `npm test` now **373 tests** green (30 files); typecheck clean; `npm run demo` and `npm run build` compile (`/brand` 3.22 kB). **Deferred (not pulled forward):** wiring the stored guide into the review's `brand_context` (so Juror 1 actually references it) is Day 27; the brand-voice embedding cache (write/read, so the guide isn't reprocessed per review) is Days 29–30 — editing the guide will invalidate that cache then; a full company **settings** surface is Day 50.
- **Brand voice in reviews (Day 27)** — The stored brand guide now actually reaches the review (DailyPlan Day 27; PRD §5 juror 1, §6.1; Architecture §2 step 2). `createReview` (`src/lib/reviews/create-review.ts`) loads the company's `brand_profiles` row server-side via a new injectable `getBrandProfile(companyId)` dep (wired in the route to `getBrandProfileByCompany` through the **request-scoped anon client**, so `brand_profiles_select` RLS scopes the read to the caller's company; the session `companyId` is passed explicitly, never the client — Rules.md §5), resolves it to `brand_context`, and threads it into `runReview`. **Scoping to Juror 1:** the orchestrator's `buildUserPrompt` now injects the brand block into the **Brand Voice Guardian's prompt only** — that is the one juror whose lens is brand voice (PRD §5, Rules.md §3), and sending the full guide to all five would waste tokens on lenses that ignore it (Rules.md §1 cost discipline). A new pure helper `resolveBrandContext(profile)` (`company/brand-profile.ts`) returns the trimmed guide text or `null`; a null/whitespace-only/absent guide makes Juror 1 fall back to inferring a professional baseline (matching its system prompt), and no other juror sees a brand block at all. **Cost model corrected (`ai/cost.ts`):** because brand context reaches a single juror, `estimateReviewInputTokens` now counts it **once** on top of the fan-out (`(content + overhead) × jurors + brandContext`) instead of multiplying it across every juror — so even a max-length review with a max-size (`MAX_TONE_GUIDE_CHARS` 8000) guide stays within `MAX_REVIEW_INPUT_TOKENS`, keeping the budget from contradicting the advertised content limit; the redacted usage log now includes the brand-context tokens. **Resilience:** the brand read **fails open** — a read blip degrades to a brand-less review (the juror infers a baseline) rather than failing the request, logged with redacted context (no content/secret — Rules.md §6). New tests: 3 in `tests/orchestrator.test.ts` (the guide reaches only Juror 1; the no-guide baseline note is Juror-1-only; the recorded prompt matches `buildUserPrompt`), 4 in `tests/reviews-endpoint.test.ts` (guide loaded + injected into Juror 1 only + read scoped to the session company; no-guide and whitespace-only → baseline; brand read failure fails open with a redacted log), 1 in `tests/company-brand-profile.test.ts` (`resolveBrandContext`), and the Day 18 cost tests updated to the once-counting semantics (incl. a new assertion that a max content + max guide stays in budget). No new dependencies; no live-credential needs (offline mock + injected deps). `npm test` now **382 tests** green (30 files); typecheck clean; `npm run demo` and `npm run build` compile. **Deferred (not pulled forward):** the brand-voice **summary/embedding cache** (so the guide isn't reprocessed on every review, and is invalidated on edit) is Days 29–30 — today passes the full guide verbatim to Juror 1; Week 4 hardening is Day 28.

### 🚧 In progress / next up (Phase 3 → Week 4)
- [ ] **Owner to add real keys** in `.env.local` (Supabase + `AI_API_KEY`) to run the live model, apply the migration, and exercise signup/login end-to-end against a real Supabase project (the cloud build has no secrets, so auth is code-complete but unverified against a live DB).
- [ ] Light calibration of persona prompts against real model output once keys are in.
- [ ] **Weeks 1–3 complete (Days 1–21); Week 4 in progress (Days 22–27).** The submit → scorecard → history → detail flow is built, state-complete, and the core engine is hardened: suggested-rewrite UX (Day 15), per-company juror weights (Day 16), per-plan rate limiting (Day 17), cost guardrails (Day 18), provider resilience (Day 19), platform-aware SEO/stop-scrolling prompts (Day 20), and a Day 21 week-end hardening pass. **Week 4 (multi-tenant foundations) is underway:** Day 22 shipped first-admin company onboarding (name/industry/plan-tier flow + `onboarded_at` gate), Day 23 the admin-only team management list (`/team`, members + roles, tenant-safe + admin-gated), Day 24 the invite-by-email token flow (`invitations` table with a stored token **hash**, admin invite form + stubbed-email link, and a standalone `/invite/<token>` accept/signup page that joins the invitee to the existing company), Day 25 role management (admin promote/demote from the team table, server-authorized via `changeMemberRole` with a `wouldRemoveLastAdmin` no-lockout guard and a company-scoped `updateUserRole` write), Day 26 brand profile CRUD (admin-only `/brand` editor for the company's tone/style guide, `saveBrandProfile` create-or-edit core, RLS-gated write), and Day 27 brand voice in reviews (the stored guide is loaded server-side in `createReview` and injected into **Juror 1's** prompt only, counted once in the token budget, failing open on a read blip). Next is **Day 28 — Week 4 hardening** (tests + review; Memory.md; Week 4 summary). The admin UI to *edit* juror weights (calling `updateCompanyJurorWeights`) is still to come (PRD §6.3); the brand-voice **summary/embedding cache** (Days 29–30) will avoid reprocessing the guide per review and be invalidated on edit — today Juror 1 gets the full guide verbatim; a full company **settings** surface to edit onboarding fields post-setup is Day 50.

### ⏭️ Later phases (not started)
- Phase 2 — Core review engine (submit UI, review endpoint, scorecard, history).
- Phase 3 — Multi-tenant & dashboard (teams/roles, brand upload + embedding cache, analytics, export, cost controls, security pass).
- Phase 4 — UI/UX polish (landing page, brand pass, responsive/a11y, user testing).
- Phase 5 — Billing scaffold, pricing, deploy, portfolio writeup, demo video, outreach.

See [Phases.md](Phases.md) for full deliverables and acceptance criteria.

---

## 4. Decisions Log

Record notable choices here so they aren't relitigated. Newest first.

| Date | Decision | Rationale |
|---|---|---|
| 2026-09-22 | **Brand profile CRUD is a pure `brand-profile.ts` + an injectable `saveBrandProfile` create-or-edit core + a company-scoped `updateBrandProfile` write; the write goes through the request-scoped client so the admin-write RLS policy is the DB guard, unlike the other company writes** (Day 26) | The DoD is "brand guide saves; admin-only via RLS", so the split follows the Day 23–25 pattern: the testable value (the input contract + length bounds, the display predicates/helpers) is pure in `brand-profile.ts`; the decision/authz + create-vs-edit logic is an injectable core (`save-brand-profile.ts`) mirroring `update-role.ts`; the DB write is in `queries.ts`; the editor is a small client island. **The write client is the key divergence:** every other Week-4 company write (companies/users/invitations) uses the **service role** because those tables have *no* client write policy — but `brand_profiles` uniquely ships an **admin-only RLS write policy** (`brand_profiles_write`, from the Day 1 migration). So rather than bypass RLS with the service role, this write goes through the **request-scoped client** and RLS is the DB-level guard, which is the most faithful reading of "admin-only via RLS" and actually exercises the existing policy. The core's 403 (a member never reads or writes) + the page `requireAdmin()` are defense-in-depth and the directly-testable trust boundary. **Create-or-edit, not insert-only:** a company has one guide, so the core reads the current profile and updates it in place (scoped by id AND company_id, so a write can't touch another company's row) or inserts the first — replacing the Day 2 insert-only stub that would have left stale history rows. `updated_at` is bumped explicitly (an UPDATE doesn't re-run the default), and `embedding_ref` is left untouched because the brand-voice cache is Days 29–30 (editing the guide will invalidate it then). Tenancy is server-derived throughout (company id from the session, never the form — Rules.md §5), and the guide text is never logged (Rules.md §6). Wiring the guide into `brand_context` so Juror 1 references it is Day 27. |
| 2026-09-21 | **Role management is a pure `wouldRemoveLastAdmin` guard + an injectable `changeMemberRole` core + a company-scoped `updateUserRole` write; self-demotion is allowed but the company can never reach zero admins** (Day 25) | The DoD is "role changes persist and are authorized on the server", so the split follows the Day 23/24 pattern: the testable value (input contract, the last-admin predicate, the toggle labels) is pure in `role-management.ts`; the decision/authz logic is an injectable core (`update-role.ts`) mirroring `create-invitation.ts`; the DB write is in `queries.ts`; the per-row toggle is a small client island. **Authz on the server, not the UI (Rules.md §5):** the core returns 403 for a member (never reading or writing) even though the page already `requireAdmin()`s, because a pure core check is directly testable and is the real trust boundary. **Tenancy is server-derived:** the target is resolved from the company's own session-scoped roster (so a foreign/absent user is an indistinguishable 404 — never reveal cross-tenant existence, Rules.md §6), and `updateUserRole` scopes the write by **both `id` and `company_id`** so a service-role write can't touch another company's user even if handed a foreign id. **The no-lockout invariant** is `wouldRemoveLastAdmin`: a demotion of the sole admin is blocked (409) rather than blocking all self-changes — the more flexible, correct model is that an admin *can* step down to member as long as another admin remains, which real products (GitHub/Slack owner rules) follow; the guard, not a blanket self-block, is what prevents a company locking itself out. Same-role is an idempotent 200 no-op (no write). The write runs under the service-role client because `users` has no client write policy (the Day 16/22/24 write pattern). |
| 2026-09-20 | **Invitations store only a SHA-256 token hash; the accept flow is a shared injectable core that joins the invitee to the invite's company, and a new invitee signs up via a dedicated action that does NOT provision a new company** (Day 24) | The invite token is a **bearer credential** (whoever holds the link can accept), so it is treated like a password-reset token: only its SHA-256 hash is stored (`token_hash`), and lookups hash the presented token — a DB leak yields no usable invite (Rules.md §4). The testable value is pure/decision logic, so it splits the established way: token/lifecycle/schema/display in a pure `company/invitations.ts`; the DB writes in `queries.ts`; and three injectable cores (`create-invitation`, `accept-invitation`, `read-invitations`) mirroring `read-team.ts`, so every authz/tenancy/expiry/email-match branch is node-testable offline. **Tenancy is server-derived**: create takes `company_id`/`invited_by` from the admin session (never the client), and accept adds the member to the **invitation's** `company_id` with the invitation's role — the DoD. **Fail-closed accept guards:** unknown token → 404 (never reveal which tokens exist), expired/used/revoked → 410, and the accepting account's email **must match** the invited address (403) so a leaked link can't be redeemed by another account. The model is **one company per user**, so an already-provisioned caller is refused (409) rather than silently moved between tenants — which also forces the key decision that a brand-new invitee needs a signup path that **doesn't** auto-provision a new company (normal `signUpAction` does): `signUpViaInviteAction` creates the auth account **for the invitation's email** (no client email trusted) and joins via the same accept core. **Email is stubbed** (Non-Goal-adjacent; no transport wired): `createInvitationAction` logs the link and returns it for the admin to copy in-app, which is a real, usable delivery for now. `accept-invitation` marks the invite accepted as a best-effort follow-up (logs-but-succeeds on failure) because the member row already existing is what satisfies the DoD. Revoke/resend and a real email transport are deferred. |
| 2026-09-19 | **Team management is a read-only admin list on the Day 2 `listUsersByCompany` query, with the roster read core (`read-team.ts`) enforcing the admin role (403) as defense-in-depth, not just the page `requireAdmin()` gate** (Day 23) | The DoD is "admins see their company's members (tenant-safe)", so Day 23 is a read-and-render surface — no new table, no write path (invitations/role changes are Days 24–25). The testable value (role labels, the ordered roster, the counts) is pure, so it lives in `src/lib/company/team-view.ts`; the injectable read core lives in `read-team.ts` mirroring `read-reviews.ts`; the hook-free table in `components/company/team-list.tsx` mirroring `HistoryList` — the same pure-module + injectable-core + component split used all through Weeks 2–3, so the whole thing is node-testable offline. Tenancy comes from the **server-resolved session** passed explicitly to the query through the request-scoped anon client (RLS primary), never the client (Rules.md §5). The core re-checks `role === "admin"` and returns 403 (never revealing the roster) even though the page already calls `requireAdmin()`, because admin-only actions must be enforced on the server, not merely hidden in the UI (Rules.md §5) — and a pure core check is directly testable (the page's redirect isn't). The roster is ordered **admins-first then earliest-joined** as a pure function of the data (stable regardless of query order), and `formatJoinedDate` reuses Day 11's `formatHistoryDate` so dates read identically app-wide (Design.md §1.4). Colour never travels alone — the role badge carries its word (Design.md §6). |
| 2026-09-18 | **Company onboarding is an explicit `onboarded_at` signal + a pure `company/onboarding.ts`; the write is service-role + admin-gated, and the `(dashboard)` layout redirects an un-onboarded admin into `/onboarding`** (Day 22) | The company already exists after signup (name only, Day 3), so onboarding is a *complete-your-profile* step, not a create step — modelled with an explicit nullable `onboarded_at` (null = needs onboarding) rather than inferring it from `industry IS NULL`, mirroring Day 16's explicit `juror_weights` signal and keeping `needsOnboarding` a single pure predicate the route gate + tests share. The testable value (option lists, the input contract, the predicate, the label/allowance helpers) is pure, so it lives in `src/lib/company/onboarding.ts` with the DB write in `queries.ts` and the action in `actions.ts` — the established pure-module + injectable-write split. The write runs under the **service-role client** because `companies` has no client write policy (writes are server-side; the Day 5/16 decision), and `completeOnboardingAction` uses `requireAdmin()` so only an authenticated admin can onboard and `company_id` comes from the resolved session, never the form (Rules.md §5). The onboarding **route is standalone (outside `(dashboard)`)** so the layout's "admin needs onboarding → redirect" gate can't loop; members are never redirected (the app works with the signup name; their admin onboards the company). `planReviewAllowance` reads `PLAN_RATE_LIMITS` so the UI's per-tier "reviews/day" can't drift from Day 17. Plan tier is a **scaffold selection, not a paid entitlement** (billing is Week 11); a full editable company **settings** surface is Day 50 (onboarding is re-reachable by URL to edit for now). |
| 2026-09-17 | **Week 3 hardening is a shared no-fabrication rewrite-calibration fragment + an orchestrator integration suite + compliance-veto verdict coverage; no new product surface** (Day 21) | Mirrors the Day 7 / Day 14 week-end hardening pattern: strengthen tests and calibration rather than add surface. The calibration is a single `REWRITE_GUIDANCE` block composed into every prompt (like Day 7's `CONFIDENCE_GUIDANCE`) because the gap was cross-cutting — the contract required a `suggested_rewrite` but never constrained its content, so a rewrite could invent claims/prices/disclaimers, and the compliance juror could "fix" a claim by fabricating substantiation (adding risk, not removing it). Keeping it shared means one place to tune and one assertion in `personas.test.ts`. The orchestrator suite exists because the Day 20 platform injection and the Day 19 "nudge only on output error" invariant were tested in isolation / asserted only in comments; driving the whole `runReview` with a recording client that captures each `client.complete` call proves them end-to-end and stops a refactor from silently appending a nudge to a transient-failure retry (which would waste a real prompt) or dropping the platform block. The extra verdict cases fill the Phase-2 acceptance criterion's compliance-veto gaps, and pin a multi-tenant-safety invariant: the veto is a risk gate on the *verdict*, not a term in the weighted aggregate, so a company can't disable it by setting `compliance_legal_flagger` weight to 0. Prompt wording still needs live-model calibration (owner key). |
| 2026-09-16 | **Platform-awareness is a pure `personas/platform-guidance.ts` injected into only the SEO + stop-scrolling user prompts; other jurors unchanged** (Day 20) | The two lenses whose expectations genuinely shift by channel are discoverability and hook strength (PRD §5 jurors 4 & 5), so only those two are platform-aware — brand voice, compliance, and audience fit judge the same way regardless of platform, so widening them would be scope creep. Platform is a per-review runtime value, so it can't live in the static persona `systemPrompt`; the concrete per-platform guidance is injected at user-prompt assembly time (`buildUserPrompt`, now exported for testing). The mapping is pure and lives under `personas/` because prompt content is reviewed code (Rules.md §3) and to match the injectable-core/pure-module pattern (`scoring.ts`, `cost.ts`, `resilience.ts`). `platformGuidanceFor` returns a curated block for a known platform, a generic platform-naming nudge for an unrecognized-but-named platform (so the prompt still *reflects* the platform — the DoD — rather than silently ignoring it), and `null` when there's no platform or the persona isn't platform-aware (leaving those prompts byte-for-byte unchanged, so the existing suites stay valid). `normalizePlatform` folds aliases (`twitter`→`x`, etc.) so free-form platform values still hit the curated guidance. Calibrating the wording against real model output waits on a live key. |
| 2026-09-15 | **Provider resilience is a pure `ai/resilience.ts` (per-call timeout via `AbortSignal` + exponential backoff), wired into `runPersona` and kept distinct from the invalid-JSON retry; only transient failures retry** (Day 19) | The testable value is pure — an error classification, a backoff computation, a timeout wrapper, and a retry loop — so it lives in `src/lib/ai/resilience.ts` with the orchestrator wiring it, matching the injectable-core pattern (`persist`/`client`/`getRateUsage`/`cost.ts`). The **timeout aborts an `AbortSignal`** threaded into `client.complete` (and on to the Anthropic SDK's `{ signal }`) so a hung call is genuinely cancelled, not just abandoned — a hang becomes a bounded, retryable `TimeoutError`. Backoff retries **only** `classifyModelError`-transient failures (timeout, 429, 5xx, and unclassifiable-as-`network`) and never a permanent non-429 4xx (a bad request/invalid key — retrying burns spend for nothing); an unknown error defaults to retryable because a bounded retry is cheaper than failing fast on a recoverable blip. The two resilience concerns stay **separate**: the provider timeout/backoff is the network layer, while the existing Rules.md §6 "invalid JSON → retry once with a corrective nudge" is the output-contract layer — the nudge is now sent only when the previous failure was invalid *output* (`isOutputError`: ZodError/SyntaxError), since a nudge can't fix a dead connection. A juror that exhausts its retries degrades to a clean `error` slot with a content-free internal reason (the UI shows its own generic message + the Day 13 degraded banner — no provider internals leak, Rules.md §6). Timers/randomness are injectable so backoff/timeout tests are deterministic; `runReview` takes an optional `resilience` dep (defaults to production `DEFAULT_RESILIENCE`) so tests inject a no-wait policy. No circuit-breaker/per-plan timeout tuning — unneeded at this scale; structured monitoring beyond redacted logs is Day 48. |
| 2026-09-14 | **Cost guardrails live in one pure `ai/cost.ts` (size cap + token budget + redacted usage log); `MAX_CONTENT_CHARS` becomes the single source of truth, and the token budget has headroom above the char cap** (Day 18) | The testable value is pure — a size decision, a budget decision over an estimated fan-out cost, and a redacted log record — so it lives in `src/lib/ai/cost.ts` with `createReview` wiring it, matching the injectable-core pattern (`persist`/`client`/`getRateUsage`). Both guards run **before** the rate-limit read and the five model calls, so an oversized/over-budget request costs nothing (cost discipline, Rules.md §1). Token counts are a deliberately coarse `~4-chars-per-token` estimate — for budgeting/observability, never billed. The **size cap is the binding content limit** (`MAX_CONTENT_CHARS`, 10k) and is now imported by the request schema and the form counter so counter/client/server can't drift; the **token budget is a secondary fan-out ceiling** (`MAX_REVIEW_INPUT_TOKENS`, 20k) sized with clear headroom above a max-length five-juror review so it never rejects content the UI says is valid — it exists to catch a genuinely oversized fan-out once brand context (Day 27) or more jurors are added, and is unit-tested directly for that case. Redaction is a **compile-time property**: `logReviewUsage` takes a `ReviewUsageLogMeta` of only IDs/counts/scores, so there is no field to put the user's content or a secret in (Rules.md §3/§6); a test asserts the record's key set and that content/secret strings never appear. `console.info` (usage is informational, not an error); the success-path suites silence it in `beforeEach` to keep output clean. |
| 2026-09-13 | **Rate limiting is a rolling per-plan window, enforced in `createReview` before the fan-out via an injected usage reader; fails open** (Day 17) | The testable value is the policy + decision math, so it lives in a pure `rate-limit.ts` (`PLAN_RATE_LIMITS`, `evaluateRateLimit` → `{allowed, remaining, resetAt, retryAfterSeconds}`) with the DB count injected as `getRateUsage` into `createReview` — same injectable-core pattern as `persist`/`client`. Enforcing **before** the five model calls is the whole point (cost discipline, Rules.md §1). A **rolling** window (not a calendar day) makes the reset honest — the earliest a slot frees is when the oldest counted review ages out (`oldest + windowMs`) — and one exact-count query gets `count`+`oldest` cheaply (the existing `reviews(company_id, created_at desc)` index covers it, no migration). The plan comes from the **server-resolved** company row, never the client (Rules.md §5); an unknown plan falls back to the most restrictive `free`. The usage read **fails open** (allows the review, logs redacted) because rate limiting is a soft cost guard, not an authz/tenancy boundary — blocking paying users on a transient count blip is worse than a rare over-allowance, and the persist step still runs. `getRateUsage` is optional on `createReview` so the existing endpoint/weights tests (which exercise other paths) stay byte-for-byte valid; the route always wires it. |
| 2026-09-12 | **Per-company weights: a `Partial` config resolved onto the equal default, threaded from the session's company row; `computeAggregate` takes an optional weight map** (Day 16) | The testable domain logic is the resolve + weighted mean, so it lives in `scoring.ts` (`resolveWeights` → full `PersonaWeights`, `computeAggregate(jurors, weights?)`) with the config *shape* in `schema/weights.ts` (`PersonaWeightsInputSchema`) — same schema/domain split as `juror.ts`/`scoring.ts`. `computeAggregate` defaults `weights` to `DEFAULT_PERSONA_WEIGHTS` so every existing caller/test is byte-for-byte unchanged (the DoD's "default unchanged"). The stored config is a **partial** map merged onto the default (unspecified jurors keep 0.2, an explicit 0 disables a juror), and `resolveWeights` treats it as untrusted jsonb — bad values are ignored and an all-zero/empty result falls back to equal, so scoring never divides by zero (Rules.md §3/§6). It's read from the **server session's** `company.juror_weights` in `createReview`, never the client (Rules.md §5), and since `SessionContext` already carries the full `CompanyRow` there's no extra DB read. The admin edit UI is deferred (PRD §6.3); Day 16 scaffolds the column (nullable jsonb, null = default, no backfill), the `CompanyRow` field, the validated `updateCompanyJurorWeights` write path, and the read wiring only. |
| 2026-09-11 | **Rewrite diff is a pure LCS over whitespace-preserving tokens; original threaded from the review, not the juror** (Day 15) | The testable value (vitest env is `node`) is the diff logic, so it lives in `rewrite-diff.ts` (`computeRewriteDiff` → `{changed, tooLarge, segments, added, removed}`) with colour→Tailwind and the copy client-island at the component layer — same lib/component split as `review-form.ts`/`scorecard-view.ts`. Tokenising into whitespace/non-whitespace runs (not just words) lets the before/after streams reconstruct the inputs exactly, which is what the tests assert. The rewrite is a rewrite of the *whole submitted content*, so `original` is the review's `content_text` threaded `Scorecard → JurorCard → RewritePanel`, not a per-juror field — added to `PersistedReview` for the detail page (single-review read, so returning content is intentional; the history `ReviewSummary` stays thin) and captured from form state on a fresh submit (the API response doesn't echo content). `changed:false` hides the comparison when there's nothing to compare (missing original or rewrite==original, which PRD §7 explicitly allows); a 400-token/side cap (`tooLarge`) falls back to plain blocks so a long rewrite stays legible and the DP table bounded. Every highlight is paired with a text cue + an sr-only count summary (Design.md §6). |
| 2026-09-10 | **Accessibility spot-check encoded as executable invariants, not a one-off manual pass** (Day 14) | The vitest env is `node` (no jsdom/RTL), so the durable, regression-proof way to hold the Design.md §6 "never colour-only" rule is to test the *logic* the paired labels and colours come from, exhaustively over every value: every score band has a tier word, every verdict/severity/confidence has a spoken label, and every `Tone` maps to a real class in all four component maps (so a signal never renders style-less). The spot-check also surfaced a real inconsistency — the `Scorecard` header showed the raw `platform` ("instagram") while history showed the human label ("Instagram") — fixed by reusing the shared `contentTypeLabel`/`platformLabel` from `history-view.ts` rather than the duplicated local copy, so both surfaces read a review identically (Design.md §1.4 "consistency is credibility"). No new product surface, matching the Day 7 Week-1 hardening pattern. |
| 2026-09-09 | **States handled with Next.js route conventions + a pure banner helper, reusing existing states** (Day 13) | The flows already had most states (submit maps network/validation/malformed failures, history/detail have empty + 404 + retry-alert states, `JurorCard` renders an errored slot). Day 13 filled the gaps with the framework's own primitives rather than bespoke wrappers: `loading.tsx` Suspense fallbacks for the two async pages, a group-level `error.tsx` boundary as the backstop for *unhandled* throws (expected outcomes are still mapped in the pages), and a group-level `not-found.tsx` for the detail page's `notFound()`. The only new logic — the scorecard's degraded-review banner — went into `scorecard-view.ts` (`summarizeJurorHealth`/`jurorHealthNotice`, pure + node-tested) with colour→Tailwind at the component layer, keeping the established lib/component split. Skeleton pulse is `motion-safe:`-gated so the cue never depends on motion (Design.md §6); the error boundary logs a digest only, never content/secrets (Rules.md §6). |
| 2026-09-08 | **Detail page reuses the Day 6 read core + Day 9/10 Scorecard, no new endpoint; a 401 collapses into the 404** (Day 12) | `/review/[id]` is a Server Component, so it calls `getReviewForCompany` (the same core behind `GET /api/reviews/[id]`) directly with the request-scoped anon client rather than fetching its own JSON API — one tenancy path (session-derived company + RLS), no round-trip, mirroring the Day 11 history page. The scorecard needed zero visual work: the Day 9/10 `<Scorecard>` is hook-free and already accepts the loose `PersistedReview` shape with the full 5-tier colour/tier/compliance system. The only new logic — mapping the core's result to a render branch — went into a pure `review-detail-view.ts` (`reviewDetailState`) so it's node-testable like the other view modules. A 401 (session couldn't re-resolve on an already-gated page) is folded into `not-found` alongside the 404 so the page never reveals whether the id exists for another tenant (Rules.md §5/§6); a 500 stays a distinct retry `role="alert"` so a transient DB blip isn't shown as a missing review. |
| 2026-09-07 | **History reuses the Day 6 read core + a pure view module, no new endpoint** (Day 11); the Server Component reads directly | `/history` is a server page, so it calls `listReviewsForCompany` (the same core behind `GET /api/reviews`) directly with the request-scoped anon client rather than fetching its own JSON API — one tenancy path (session-derived company + RLS), no round-trip. Presentation went into `history-view.ts` (pure, node-testable) reusing `verdictTone`/`scoreTone`/`scoreTierLabel` so a history row's pill/chip match the scorecard exactly; `formatHistoryDate` is built from UTC parts (not `toLocaleDateString`) so it's deterministic across test/runtime locales. Rows link to `/review/[id]` (Day 12) now, so Day 12 only has to build the page. A never-scored review reads neutral with no tier word (Rules.md §6). |
| 2026-09-06 | **Verdict/score visual system built on the Day 9 seam, colour never travels alone** (Day 10); compliance-flag override is issue-gated | Extending `scorecard-view.ts` (pure) + `tone-classes.ts` (classes) kept the split intact: the 5-tier band, the `hasComplianceFlags`/`jurorScoreTone`/`issueTone` override logic, and the tier labels are all unit-testable in node, colour stays at the component layer. The compliance override fires only when the compliance juror actually flags an issue (not merely because it's the compliance persona), so a clean compliance pass keeps its honest score colour — Burgundy means "there is a flag." Every colour is paired with text (tier word on each chip, "Compliance flag" badge, `sr-only` severity/verdict prefixes) per Design.md §6; the app-wide AA verification stays Day 62. `warning-deep` added as `warning.deep` in Tailwind so `bg-warning` still resolves. |
| 2026-09-05 | **Scorecard/JurorCard split into a pure view module + hook-free components** (Day 9); score→colour kept coarse | Display logic (`scorecard-view.ts`) is unit-testable in the node env like `review-form.ts`; colour→Tailwind lives at the component layer (`tone-classes.ts`) so the lib stays framework-agnostic. The components are hook-free so they render in both the client form and the server detail page (Day 12), and take the loose `ReviewResult`/`PersistedReview` shape so a stored review renders identically. The chip/pill mapping is deliberately coarse — the full 5-tier system, compliance override, issue-severity colours, and a11y AA pass are Day 10 (not pulled forward), mirroring how Day 8 deferred the scorecard itself. |
| 2026-09-04 | **Submission-form logic factored into a pure module** (`review-form.ts`), with a deliberately minimal Day 8 result panel (Day 8) | The vitest env is `node` (no jsdom/RTL), so the testable value is the pure logic — option lists, `validateReviewForm`, and `submitReview(fetchImpl)` — matching the `createReview`/`evaluateAccess` pattern; the React component stays a thin state+render shell. `submitReview` re-validates the response with `ReviewResultSchema` (client validation is convenience, the server stays the trust boundary). The result panel shows only aggregate/verdict/juror rows + raw JSON so the full Scorecard/JurorCard (Days 9–10) isn't pulled forward. |
| 2026-09-03 | **Confidence rubric lives in a shared prompt fragment**, not per-persona (Day 7) | `confidence` was in the JSON contract but no persona told the model when to use `high/medium/low`, so it varied. A single `CONFIDENCE_GUIDANCE` block composed into every prompt (like `SHARED_ANCHORS`) keeps the field comparable across jurors with one place to tune it, and is asserted by `tests/personas.test.ts`. Calibration against real model output still waits on a live key. |
| 2026-09-03 | **Tenancy proven at the data layer with a fake query builder**, not only via the injectable cores (Day 7) | The Day 5/6 tests injected `persist`/`fetch`/`list`, so nothing asserted that `queries.ts` actually applies the `company_id` filter or rolls back a half-written review. A small chainable Supabase fake that records `.eq()`/ops closes that gap (read scoping + write rollback) without a live DB, complementing the end-to-end 404-not-403 cross-tenant check. |
| 2026-09-02 | **Review reads use the request-scoped anon client (RLS), not the service role** (Day 6) — the mirror of the Day 5 write decision | Unlike writes, both `reviews` and `persona_scores` have SELECT policies, so RLS is the natural primary tenancy guard for reads and no service-role key is needed. The session `companyId` is still passed explicitly to `getReviewById`/`listReviewsByCompany` as defense-in-depth, and an absent/other-company row returns 404 (never reveal cross-tenant existence). The endpoint returns a reconstructed `PersistedReview` (not raw rows) so a stored review renders like a freshly-run one; the list returns a thin `ReviewSummary` that omits `content_text`. |
| 2026-09-01 | **Review writes use the service-role client**, not the request-scoped anon client (Day 5) | The RLS migration gives `persona_scores` a SELECT policy but no INSERT policy, so an anon/authenticated session cannot write scores. The `insertReviewWithScores` path already takes an explicit `companyId`; the endpoint derives it from the session and writes server-side with the service role. Tenancy is enforced by the session, not RLS, on this path. |
| 2026-09-01 | **Endpoint logic factored into `createReview` (injectable deps)** with a thin route wrapper (Day 5) | The route touches `next/headers` (cookies) and service-role env; injecting `{session, persist, client}` lets the whole flow — tenancy derivation, 4xx/502/500 mapping — be unit-tested offline with the mock client, matching the pure-core pattern used for `evaluateAccess`/`jurorSlotToScoreRow`. |
| 2026-08-31 | **Two-layer route protection** (Day 4): middleware does a coarse auth-presence gate; role (admin) checks live in Server Component guards, not middleware | The user's `role` is a DB column, not a JWT claim, so resolving it in Edge middleware would mean a per-request DB read there. Keeping admin authz in `requireAdmin()` (server components) is safer defense-in-depth and matches Rules.md §5 (enforce admin actions server-side). The pure `evaluateAccess` policy is shared and unit-tested so both layers agree. |
| 2026-08-30 | Add **`@supabase/ssr`** for cookie-based auth sessions (Day 3) | Standard, actively-maintained Supabase package for Next.js App Router; removes hand-rolled SSR cookie/session handling so the server can read the session and RLS applies. Sessions live in cookies (not localStorage) so they persist across server renders. |
| 2026-08-30 | Signup provisions company + admin via **service-role client**, not the anon session | RLS's `auth_company_id()` needs an existing `users` row to resolve; the first company + admin rows must bootstrap that state, so they're written server-side with the service role and an explicit (never client-supplied) `userId`. |
| 2026-08-29 | Recommend **Next.js + Supabase** single stack (vs. separate React + Express backend) | Fewer moving parts for a solo dev; one deploy target; Supabase bundles Postgres + Auth + RLS. |
| 2026-08-29 | Default AI model **Claude Haiku 4.5**, provider abstracted | Cost-efficient; matches README's "one cheap/fast model" mechanic; swappable via `lib/ai/client.ts`. |
| 2026-08-29 | **Compliance juror can veto** to `fail` regardless of aggregate | Legal risk should block publishing even if other scores are high. |
| 2026-08-29 | Authored full docs set before writing code | Locks contracts (JSON schema, data model) that everything else depends on. |

---

## 5. Gotchas & Watch-outs

- **Never break the juror JSON contract** without updating [PRD.md](PRD.md) §7, the Zod schema, and the persistence mapping together.
- **Multi-tenancy:** always derive `company_id` from the session server-side; never trust the client. RLS on every tenant table.
- **Secrets:** only `.env.example` is committed; real keys live in `.env.local` and hosting dashboards. AI/service-role keys are server-side only.
- **Cost:** don't fan out to multiple paid providers per review — that defeats the whole point.

---

## 6. How to Update This File

At the end of a session: move finished items from "In progress" to "Done," add anything non-obvious to the Decisions Log, and bump the **Last updated** date. Keep it skimmable — this is the file that saves everyone from reading the whole repo.

---

_Related: [PRD.md](PRD.md) · [Architecture.md](Architecture.md) · [Rules.md](Rules.md) · [Phases.md](Phases.md) · [Design.md](Design.md)_
